# 多模态大模型从理解到生成：原理与演进全解

## 一、阅读地图与基础概念

> 本文从**理解**和**生成**两条主线出发，围绕公开论文中的代表性模型拆解关键设计。模型尺寸均以“代表性示意”或论文公开配置为准，不应外推到同系列其他版本；2025 年后的架构变化较快，文末给出原始报告以便核对。
>
> **版本与范围说明**：本文讲的是架构原理，不是产品能力排行榜。`Qwen-VL` 特指 2023 年论文版本，`Janus` 特指初代 Janus，`Qwen2.5-Omni`、`BAGEL` 与 `Ming-Omni` 以各自公开技术报告为边界；同名系列的后续版本可能已经更换视觉编码器、tokenizer、分辨率策略或训练目标。

### 1.1 基础概念（精简版）

|概念|通俗解释|打个比方|
|---|---|---|
|**模态（Modality）**|信息的一种形式或通道，比如文字、图像、音频、视频。|就像人的五种感官，每种感官接收一种信息。|
|**Token（词元）**|模型一次处理的离散符号或连续位置单元。文本 token 通常是词表 ID；“图像 token”可能是 patch 特征、VQ code，也可能只是连续 latent，三者不能混为一谈。|像不同制式的积木：都能排成序列，但接口和信息量不同。|
|**Embedding（向量表示）**|把离散 ID 或连续特征映射到主干隐藏维度。向量邻近是否代表语义相近，取决于训练目标与所在空间。|把不同输入换算到模型可运算的坐标系。|
|**潜空间（Latent Space）**|用更低维表示保留任务所需信息。图像/音频生成中的自编码 latent 通常是**有损压缩**，可重建不等于逐像素无损。|像有损媒体编码：体积更小，但会牺牲部分细节。|

为避免后文把不同层次混在一起，可以把多模态系统拆成五层：

|层次|回答的问题|典型实现|
|---|---|---|
|**编码（Encode）**|每种原始信号怎样变成特征？|ViT、音频编码器、VAE、VQ tokenizer|
|**对齐（Align）**|不同特征怎样进入可比较或可共享的空间？|线性层、MLP Connector、对比学习、Cross-Attention Resampler|
|**融合（Fuse）**|不同模态在何处交换信息？|共享嵌入空间、跨模态注意力、token 拼接后的 Self-Attention|
|**推理/建模（Reason）**|谁负责跨 token 的上下文建模？|Encoder、Decoder-Only LLM、MoT|
|**输出（Decode）**|模型怎样得到文字、像素或波形？|词表头、VQ 解码器、VAE + diffusion/flow、音频 codec 解码器|

“都变成 token”只说明接口可序列化，不表示这些 token 是同一种对象：文本 token 是离散词表 ID，ViT patch token 通常是连续特征，VQ 图像 token 是离散码本 ID，扩散 latent token 则常是连续且带噪的向量。

### 1.2 理解与生成：两条始终交缠的主线

**🔍 理解（Understanding）**

输入一张图、一段音频或视频，模型要能"看懂、听懂"，并用文字回答问题。

- 典型任务：看图问答、OCR 识字、视觉推理、语音识别

- 关键诉求：抓住**高层语义** —— "这是什么、在干什么"

**🎨 生成（Generation）**

输入一句话或一张参考图，模型要能"创造"出新的图片、视频或语音。

- 典型任务：文生图、图生图、文生视频、语音合成

- 关键诉求：还原**低层细节** —— "每个像素长什么样"

**记住这组主要张力：理解更偏语义不变性，生成更偏可重建细节。**它不是绝对二分：OCR、定位也需要细节，文生图同样需要语义与构图。
后文所有统一架构（Chameleon / Janus / BAGEL / Omni）都在回答：

- 如何在一个系统里同时兼顾这两种几乎相反的需求？

### 1.3 对齐与融合的三种位置

多模态模型常被笼统地描述为“把图像接入 LLM”，但真正影响能力与成本的是**信息在哪一步相遇**：

1. **共享嵌入空间（双塔/晚融合）**：图像与文本分别编码，只在最终向量上比较。CLIP 属于这一类。它适合检索与零样本分类，但单次相似度计算不会产生细粒度跨模态推理。
2. **Connector 或 Cross-Attention（中间融合）**：视觉序列经 MLP、查询压缩器或跨注意力适配成 LLM 可读的表示，再与文本共同进入语言主干。LLaVA 与初代 Qwen-VL 分别代表 MLP Connector 与 Query Cross-Attention 路线。
3. **统一序列（早融合）**：图像、文本乃至带噪 latent 被组织进同一上下文，由共享或分专家的 Transformer 直接建模。Chameleon、Janus 与 BAGEL 都属于广义统一序列路线，但它们的视觉表示和训练目标并不相同。

无论采用哪条路线，映射到相同隐藏维度都只是**形状兼容**；是否实现语义对齐，取决于配对数据、损失函数、注意力连接和哪些参数参与训练。

### 1.4 图像、音频和视频究竟怎样变成 token？

“多模态 token”不是一种统一的数据类型，而是不同信号接入序列模型时的统称。判断一种 token 时，至少要回答四个问题：它是离散 ID 还是连续向量？一个 token 覆盖多大时空范围？它是否可逆？它由什么目标训练？

|模态与用途|token 化路径|token 的性质|主要保留的信息|
|---|---|---|---|
|文本|字符/字节序列 $\rightarrow$ BPE、SentencePiece 等 tokenizer|离散词表 ID，查表后变成连续 embedding|语言组合与符号信息|
|图像理解|图像 $\rightarrow$ patch/tubelet 投影 $\rightarrow$ ViT|连续 patch 特征，通常不可直接解码回原图|对象、文字、空间关系等任务相关语义|
|离散图像生成|图像 $\rightarrow$ VQ 编码器 $\rightarrow$ codebook 最近邻|离散 code ID，可经 VQ decoder 近似重建|受码本容量约束的纹理与结构|
|连续图像生成|图像 $\rightarrow$ VAE/Autoencoder latent $\rightarrow$ patchify|连续 latent；扩散/flow 训练时还会加入噪声|可重建的低层细节与连续生成状态|
|音频理解|波形 $\rightarrow$ 频谱/卷积前端 $\rightarrow$ 音频编码器|按时间排列的连续帧特征|语音内容、音色、环境声等|
|音频生成|波形 $\rightarrow$ neural codec / RVQ|一个或多个码本的离散 codec ID，也可能先生成连续声学特征|可解码的声学与韵律信息|
|视频理解|采样帧 $\rightarrow$ 2D patch，或视频 $\rightarrow$ 3D tubelet|带时间坐标的连续视觉特征|跨帧语义、动作与空间关系|
|视频生成|视频 VAE/tokenizer $\rightarrow$ 时空 latent/code|连续带噪 latent 或离散时空 code|可重建的外观、运动和时间连续性|

对于大小为 $T\times H\times W$ 的视频，若 3D patch 的尺寸为 $P_t\times P_h\times P_w$，三个维度都能整除对应 patch 尺寸且不使用边界填充，则原始 tubelet 数为

$$
N_{\text{video}}
=
\frac{T}{P_t}
\times
\frac{H}{P_h}
\times
\frac{W}{P_w}.
$$

这说明视频 token 数同时随时间和两个空间维度增长。实际模型还可能先降帧率、做空间 merge、Query 压缩或时空 VAE 压缩。

音频 token 数则主要由帧率决定。若编码器每隔 $\Delta$ 秒输出一帧，时长为 $T_a$ 秒的音频大约产生

$$
N_{\text{audio}}\approx \left\lceil\frac{T_a}{\Delta}\right\rceil
$$

个时间位置。若 codec 使用 $K$ 个残差码本，每个时间位置会对应 $K$ 个 code；实现可以把它们表示成 $K$ 条并行轨道，也可以串行化。两种表示的序列成本和生成依赖不同，不能只看“音频有多少 token”这个单一数字。

把这些 token 交给同一 Transformer 前，系统还必须规定：

1. **投影**：连续特征怎样映射到主干隐藏维度；
2. **位置**：图像的二维坐标、视频/音频的时间戳怎样编码；
3. **边界**：用哪些起止符区分模态、样本和时间块；
4. **注意力掩码**：哪些 token 能读取哪些 token；
5. **输出头**：隐藏状态最终进入文本 softmax、codec 预测头，还是 diffusion/flow 回归头。

因此“统一 token 序列”只是统一建模的入口，不是统一表征问题的终点。

### 1.5 代表性演进总览：先看地图，再看细节

本节只给出**时间轴 + 代表模型**的总览，作为导读地图；每个阶段的网络细节都在后文对应模型章节展开。

|阶段|核心突破|代表模型（详见后文章节）|能力边界|
|---|---|---|---|
|**视觉 Transformer 化**（2020）|统一视觉与语言的底层架构|ViT（见 2.1）|图像分类|
|**跨模态对齐**（2021）|图文嵌入空间统一|CLIP（见 2.2）|图文检索、零样本分类|
|**多模态理解**（2023-2025）|LLM 获得视觉理解能力，并走向动态分辨率与长视频|LLaVA / Qwen-VL / Qwen2-VL / Qwen2.5-VL（见 2.3—2.6）|图文问答、OCR/定位、视觉推理、视频理解|
|**理解生成统一**（2024-2025）|同一模型同时理解和生成图像|Chameleon / Janus / BAGEL（见 4.2 / 4.3 / 4.4）|图文理解 + 图像生成 + 编辑|
|**全模态系统**（2025 起的代表案例）|扩展到音频、视频与流式输出|Qwen2.5-Omni / Ming-Omni（见 5.1 / 5.2）|两者输出模态不同；“Omni”不等于所有模态都能生成|

**阅读建议：**

- 只想掌握大图景：读完本节 + 2.1/2.2/2.3，小结即可。

- 想深入理解视觉-文本多模态：重点读第二章和第四章。

- 想系统掌握生成架构：从第三章开始，逐层跟着**维度流**看完 VAE / Diffusion / U-Net / DiT。

---

## 二、理解侧核心模型

> 本章以 **ViT → CLIP → LLaVA → Qwen-VL → Qwen2-VL → Qwen2.5-VL** 为主线，从"看清一张图"到"看图说话"，统一使用**七段式模板**拆解每个模型：1）要解决的问题  2）总体网络架构图  3）逐层维度流  4）关键子模块  5）训练与损失  6）优势与局限  7）一句话总结。Qwen2.5-Omni 的视觉链路承接关系在 2.7 概述，完整全模态架构见 5.1。
>
>

### 2.1 ViT

#### 2.1.1 要解决的问题（痛点 & 背景）

在 ViT 之前，视觉领域主要依赖 CNN。CNN 很擅长抓局部纹理，但它的归纳偏置很强：天然假设相邻像素更重要、卷积核局部滑动最有效。与此同时，NLP 里的 Transformer 已经证明：只要把输入表示成 token 序列，Self-Attention 就能学到很强的全局关系。ViT 的核心问题就是：**能不能把图片也变成一串 token，让视觉和语言共享同一种主干架构？**

#### 2.1.2 总体网络架构图

```text
图像 [B,3,H,W]
  └─ 划分 P×P patch
      └─ Flatten + Linear Projection
          └─ 拼接 [CLS] + 加位置编码
              └─ Transformer Encoder × L
                  └─ 读取 [CLS] → 分类头
```

若 $H,W$ 都能被 $P$ 整除，则 patch 数

$$
N=\frac{H}{P}\frac{W}{P},
$$

每个展平 patch 的维度是 $P^2C$，投影矩阵 $E\in\mathbb{R}^{P^2C\times D}$ 把它映射到主干维度 $D$。加入 `[CLS]` 后，编码器输入为 $X_0\in\mathbb{R}^{B\times(N+1)\times D}$。

#### 2.1.3 逐层维度流（从输入张量到输出张量）

以 **ViT-Base** 为例：输入分辨率 `224×224×3`，patch 大小 `16×16`，隐藏维度 `768`，12 层 Transformer，12 个头。

```text
输入图像                 : [B, 3, 224, 224]
Patchify                 : [B, 196, 768]      # 14×14=196 个 patch；每块 16×16×3=768
Patch Embedding          : [B, 196, 768]      # 线性投影到模型维度
拼接 CLS token           : [B, 197, 768]      # 前面多 1 个可学习 CLS token
加位置编码               : [B, 197, 768]
Transformer Encoder ×12  : [B, 197, 768]      # 每层形状保持不变
Final LayerNorm          : [B, 197, 768]
取 CLS 向量              : [B, 768]
分类头                   : [B, num_classes]

```

#### 2.1.4 关键子模块详解

**A. Patch Embedding**

1. **模块作用**

    - 把二维图像翻译成 Transformer 能处理的一维 token 序列。

2. **内部结构顺序**

    - 切块（Patchify）

    - 拉平（Flatten）

    - 线性投影（Linear Projection）

    - 拼接 CLS（Add CLS Token）

    - 加位置编码（Position Embedding）

3. **矩阵/张量维度变化**

    - `[B, 3, 224, 224]`

    - 切成 196 个 patch：`[B, 196, 3, 16, 16]`

    - 每个 patch 拉平：`[B, 196, 768]`

    - 线性层 `W∈[768,768]` 投影后：`[B, 196, 768]`

    - 拼接 CLS：`[B, 197, 768]`

    - 加位置编码后：`[B, 197, 768]`

4. **为什么这样设计**

    - 标准 Transformer 本身没有卷积那样的二维局部归纳偏置；Patch Embedding 负责序列化图像，位置编码则告诉模型每个 patch 在网格中的位置。

**B. Multi-Head Self-Attention**

1. **模块作用**

    - 让每个 patch token 都能与全图其它 patch 直接交互，建模全局关系。

2. **内部结构顺序**

    - `X → Linear(Q,K,V) → 分头 → QK^T/√d → Softmax → ×V → merge head → Linear`

3. **矩阵/张量维度变化**

    - 输入：`X=[B, 197, 768]`

    - 线性映射：`Q,K,V=[B, 197, 768]`

    - 分 12 头：`[B, 12, 197, 64]`

    - 注意力分数：`Q @ K^T → [B, 12, 197, 197]`

    - Softmax 后乘 `V`：`[B, 12, 197, 64]`

    - 合并头：`[B, 197, 768]`

4. **为什么这样设计**

    - CNN 通常要通过层叠扩大感受野；全局 Self-Attention 在单层内即可建立任意两个 token 的直接信息通路，但代价是注意力矩阵随 $N^2$ 增长。

对单个注意力头，若 $Q,K,V\in\mathbb{R}^{B\times N\times d_h}$，则

$$
\operatorname{Attention}(Q,K,V)
=
\operatorname{softmax}\left(\frac{QK^\top}{\sqrt{d_h}}+M\right)V,
$$

其中 $M$ 是可选掩码。ViT 编码器通常不使用因果掩码，所以每个图像 token 可以双向读取其它位置。

**C. CLS token**

1. **模块作用**

    - 汇总整张图的全局信息，供分类头读取。

2. **内部结构顺序**

    - 初始化可学习向量 → 拼在 token 序列最前 → 每层与所有 patch 交互 → 最终取位置 0 输出。

3. **矩阵/张量维度变化**

    - `[B, 196, 768] → [B, 197, 768] → 取第 0 位 → [B, 768]`

4. **为什么这样设计**

    - 它像一个"会议记录员"：自己不对应任何区域，但负责把全场信息汇总成一份总结。

#### 2.1.5 训练方式与目标函数

ViT 最经典的训练方式是**有监督图像分类**：

- 大规模预训练 + 下游微调

- 分类头读取 CLS token 输出

- 损失函数通常为交叉熵

$$
L_{\text{cls}}=-\sum_{c=1}^{C}y_c\log p_c.
$$

#### 2.1.6 优势、局限与下一代模型动机

|维度|优点|局限 / 下一步动机|
|---|---|---|
|全局建模|Self-Attention 天然适合全局关系。|计算量随 token 数平方增长，高分辨率时很贵。|
|统一架构|证明纯 Transformer 主干在足够数据与计算下可成为强视觉基线，并推动视觉进入 Transformer 主流范式。|原始 ViT 是图像分类模型，本身没有图文对齐目标。|
|表达能力|数据足够大时，表示能力很强。|缺少 CNN 那种强归纳偏置，小数据下不占优。|

#### 2.1.7 一句话总结

**ViT 的历史意义，不只是把图片切成 token，而是用一个简洁、可扩展的纯 Transformer 方案证明视觉主干不必依赖卷积。**

### 2.2 CLIP

#### 2.2.1 要解决的问题（痛点 & 背景）

ViT 解决了"怎么用 Transformer 看图"，但没有解决更关键的问题：**图像和语言之间如何对齐？** 机器能看见一张猫的照片，却不知道它和"一只橘猫躺在窗台上"这句话是同一件事。CLIP 的目标，就是把图像和文本拉进同一个语义空间。

#### 2.2.2 总体网络架构图

```text
图像 ── 图像编码器 ── 投影 ── L2 归一化 ──┐
                                             ├─ 两两点积 / 温度缩放 → [B,B] logits
文本 ── 文本编码器 ── 投影 ── L2 归一化 ──┘
                                             └─ 图→文与文→图双向交叉熵
```

CLIP 的“双塔”在编码阶段没有 token 级 Cross-Attention。两种模态的交互发生在批内相似度矩阵上，因此它建立的是**全局向量级对齐**。

#### 2.2.3 逐层维度流（从输入张量到输出张量）

以下用 512 维投影空间作代表性示意；CLIP 的不同公开配置具有不同视觉主干与投影维度，不能把 512 当作所有变体的固定值。

```text
=== 图像塔 ===
输入图像                 : [B, 3, 224, 224]
ViT 编码器               : [B, 768]
图像投影层               : [B, 512]
L2 归一化                : [B, 512]         # 得到长度为 1 的单位向量

=== 文本塔 ===
输入文本                 : [B, 77]
Token Embedding          : [B, 77, 512]
Transformer              : [B, 77, 512]
取 EOS 向量              : [B, 512]
文本投影层               : [B, 512]
L2 归一化                : [B, 512]         # 得到长度为 1 的单位向量

=== 对比学习 ===
图像 embedding 批次      : [B, 512]         # v̂_image
文本 embedding 批次      : [B, 512]         # v̂_text
相似度矩阵               : [B, B]           # v̂_image @ v̂_text^T

```

#### 2.2.4 关键子模块详解

**A. 双塔 Projection + Normalize**

1. **模块作用**

    - 把图像向量和文本向量投影到同一个共享空间，便于直接比较相似度。

2. **内部结构顺序**

    - 图像塔输出 → 线性投影 → L2 归一化

    - 文本塔输出 → 线性投影 → L2 归一化

3. **矩阵/张量维度变化**

    - 图像侧：`[B,768] → [B,512] → [B,512]`

    - 文本侧：`[B,512] → [B,512] → [B,512]`

    - 相似度矩阵：`[B,512] @ [512,B] = [B,B]`

4. **为什么这样设计**

    - 如果不先投影到同一维度，图像和文本根本没法直接算余弦相似度。

**L2 归一化是什么？** $\hat{v}=v/\lVert v\rVert_2$，意思是把向量除以自身欧氏长度，得到单位向量。

在 CLIP 里，图像投影层和文本投影层都会先输出 `[B, 512]` 的向量，再做 L2 归一化：`[B, 512] → 单位向量`。这样计算相似度时，`v̂_image @ v̂_text.T → [B, B]` 就等价于在比较余弦相似度。

直观理解：归一化后，模型比较的是图像向量和文本向量的**方向是否接近**，而不是谁的数值更大。这样可以消除向量幅度的干扰，把相似度范围稳定在 `[-1, 1]`，也更便于后续 InfoNCE loss 计算。

**B. EOS token**

1. **模块作用**

    - 代表整句话的最终语义摘要。

2. **内部结构顺序**

    - 文本 token + 位置编码 → Transformer → 读取最后一个有效 EOS 位置向量。

3. **矩阵/张量维度变化**

    - `[B,77] → [B,77,512] → [B,77,512] → [B,512]`

4. **为什么这样设计**

    - 文本塔采用因果掩码时，EOS 是最后一个有效位置，天然最适合做整句总结。

**C. Similarity Matrix**

1. **模块作用**

    - 在一个 batch 内，把所有图文两两配对，构造对比学习目标。

2. **内部结构顺序**

    - 图像 embedding 与文本 embedding 做点积 → 除以温度 → 行列两方向各做 softmax / 交叉熵。

3. **矩阵/张量维度变化**

    - `I=[B,512]`

    - `T=[B,512]`

    - `S = I @ T^T = [B,B]`

4. **为什么这样设计**

    - 一个 batch 就能同时提供大量负样本。对角线是真配对，非对角线是错配对，训练信号非常强。

#### 2.2.5 训练方式与目标函数

CLIP 使用图文对比学习。设归一化后的图像向量为 $I\in\mathbb{R}^{B\times d}$，文本向量为 $T\in\mathbb{R}^{B\times d}$，则

$$
S_{ij}=\frac{I_i^\top T_j}{\tau},
\qquad
S\in\mathbb{R}^{B\times B}.
$$

图到文方向把每一行视为一个 $B$ 类分类问题，文到图方向把每一列视为分类问题：

$$
\mathcal{L}_{i\to t}
=-\frac{1}{B}\sum_{i=1}^{B}
\log\frac{\exp(S_{ii})}{\sum_{j=1}^{B}\exp(S_{ij})},
$$

$$
\mathcal{L}_{t\to i}
=-\frac{1}{B}\sum_{i=1}^{B}
\log\frac{\exp(S_{ii})}{\sum_{j=1}^{B}\exp(S_{ji})},
\qquad
\mathcal{L}_{\text{CLIP}}
=\frac{\mathcal{L}_{i\to t}+\mathcal{L}_{t\to i}}{2}.
$$

其中 $\tau>0$ 是可学习温度的等价写法。温度越小，softmax 越尖锐；实际实现通常学习 logit scale 以保证缩放因子为正。批内非对角项被当作负样本，因此重复描述或语义相同但索引不同的样本可能成为“假负样本”。

#### 2.2.6 优势、局限与下一代模型动机

|维度|优点|局限 / 下一步动机|
|---|---|---|
|跨模态对齐|用大规模图文对比学习建立通用共享嵌入空间。|它会“匹配”，但原始 CLIP 不会像生成式 LLM 那样输出长文本回答。|
|零样本泛化|无需额外训练即可做零样本分类。|更偏判别式任务，不擅长复杂推理与对话。|
|基础设施价值|CLIP ViT 后来成为大量多模态模型的默认视觉编码器。|这也暴露出一个问题：要不要一直依赖外置视觉编码器？|

#### 2.2.7 一句话总结

**CLIP 最重要的贡献，是展示了大规模自然语言监督与双塔对比学习可以得到可迁移的图文共享表示；它不是图文共同嵌入思想的第一次出现。**

### 2.3 LLaVA

#### 2.3.1 要解决的问题（痛点 & 背景）

CLIP 已经能把图文对齐，但它仍然更像一个"图文匹配器"，而不是一个会和人对话的助手。真正的关键问题变成了：**能不能把现成的视觉编码器和现成的大语言模型拼起来，让 LLM 直接获得视觉理解能力？**

LLaVA 的答案非常朴素：**不重造轮子，直接给 LLM 装上一双眼睛。**

#### 2.3.2 总体网络架构图

```text
图像 ── 冻结的 CLIP ViT ── patch features ── 2-layer MLP ──┐
                                                            ├─ 替换图像占位符
文本 ── tokenizer ── token embedding ───────────────────────┘
                                                            └─ Decoder-Only LLM
                                                                └─ 自回归文本回答
```

Connector 只解决 $d_v\to d_{\text{LLM}}$ 的接口与特征变换；真正让视觉条件参与回答的是后续 LLM 的因果 Self-Attention 和多模态语言建模数据。

#### 2.3.3 逐层维度流（从输入张量到输出张量）

以 **LLaVA-1.5-7B** 配方为例：CLIP ViT-L/14 作为视觉编码器，输入分辨率提升到 336×336，Connector 输出维度对齐到 7B Vicuna/LLaMA 的 4096 维词向量空间。13B 版本的语言隐藏维度不同，不能沿用下面的 4096。

```text
输入图像                  : [B, 3, 336, 336]
CLIP ViT-L/14             : [B, 576, 1024]      # 24×24=576 个视觉 token
MLP Connector 第 1 层     : [B, 576, 4096]      # Linear 1024→4096 + GELU
MLP Connector 第 2 层     : [B, 576, 4096]      # Linear 4096→4096
视觉 token                : [B, 576, 4096]
文本 token                : [B, L, 4096]
拼接后序列                : [B, 576+L, 4096]
LLM 主干                  : [B, 576+L, 4096]
逐词输出 logits           : [B, 576+L, vocab_size]

```

#### 2.3.4 关键子模块详解

**A. MLP Connector（两层线性结构）**

1. **模块作用**

    - 把视觉编码器输出的 patch 特征翻译成 LLM 读得懂的词向量空间。

2. **内部结构顺序**

    - `Linear(1024→4096) → GELU → Linear(4096→4096)`

3. **矩阵/张量维度变化**

    - 输入视觉特征：`[B,576,1024]`

    - 第一层后：`[B,576,4096]`

    - GELU 激活：`[B,576,4096]`

    - 第二层后：`[B,576,4096]`

4. **为什么这样设计**

    - CLIP ViT 输出的是视觉空间，LLM 工作在语言空间，中间必须有一座桥。两层 MLP 足够轻，却能学到非线性对齐关系。

LLaVA-1.5 的典型实现不是盲目使用视觉编码器的最后一个全局向量，而是从指定的 CLIP 隐藏层取 **patch token**（常见配置为倒数第二层并丢弃 CLS）。这样既保留 $24\times24=576$ 个空间位置，又避免只给 LLM 一个过度汇总的整图表示。这里的层选择和特征选择属于模型配置，换视觉主干时必须重新核对。

**B. 视觉 token 与文本 token 拼接**

1. **模块作用**

    - 让 LLM 把视觉信息当成一串特殊 token，与后续文本一起建模。

2. **内部结构顺序**

    - 视觉 token 准备好 → 文本 token 嵌入 → 在序列维拼接 → 送入自回归 LLM。

3. **矩阵/张量维度变化**

    - 视觉 token：`[B,576,4096]`

    - 文本 token：`[B,L,4096]`

    - 拼接后：`[B,576+L,4096]`

4. **为什么这样设计**

    - 这样做对现有 LLM 改动很小。更准确地说，投影后的视觉向量被放到词向量相同的隐藏空间并占据若干序列位置；它们不是词表中的离散 token ID。

在常见输入排布中，视觉位置位于用户文本之前。LLM 仍使用因果掩码：回答位置可以读取全部先前视觉位置，但视觉编码器本身已经用双向注意力完成了全图建模。训练时通常屏蔽图像、用户问题和系统提示位置的标签，只对助手回答 token 计算损失。

#### 2.3.5 训练方式与目标函数

LLaVA 最常见的是两阶段训练：

1. **特征对齐预训练**：冻结 CLIP ViT 与 LLM，只训练 Connector。

2. **多模态指令微调**：通常保持视觉编码器冻结，联合微调 Connector 与 LLM，使其学会看图问答、OCR、推理等任务。

目标函数本质是**语言建模损失**：

$$
\mathcal{L}_{\text{LM}}
=-\sum_{t\in\mathcal{A}}
\log p_\theta(y_t\mid x_{\text{img}},x_{\text{text}},y_{<t}),
$$

其中 $\mathcal{A}$ 表示需要监督的助手回答位置集合。

#### 2.3.6 优势、局限与下一代模型动机

|维度|优点|局限 / 下一步动机|
|---|---|---|
|工程效率|现成视觉编码器 + 现成 LLM，极大降低了训练门槛。|属于后期拼接方案，视觉和语言并非原生共同训练。|
|对话能力|用视觉指令微调把图像条件接入聊天式 LLM，方案简洁且易复现。|Connector 太轻时，复杂视觉信息容易被压缩丢失。|
|能力边界|看图问答、图文对话效果显著。|对 OCR、精细定位、高分辨率细节仍有瓶颈。|

#### 2.3.7 一句话总结

**LLaVA 的精髓，是用视觉编码器、轻量 Connector 与视觉指令数据，以较小结构改动构造可对话的视觉语言模型；它并不是最早的图文生成或视觉问答模型。**

### 2.4 Qwen-VL

#### 2.4.1 要解决的问题（痛点 & 背景）

LLaVA 证明了"ViT + Connector + LLM"这条路线可行，但它很快暴露出两个瓶颈：

- 图像分辨率不够高，细节看不清，OCR 和定位吃亏。

- 视觉 token 如果不做压缩，LLM 上下文会被迅速吃满。

Qwen-VL 要解决的，是在固定视觉 token 预算下提高分辨率，并通过 OCR、grounding 与多任务数据增强细粒度视觉能力。

#### 2.4.2 总体网络架构图

```text
448×448 图像
  └─ OpenCLIP ViT-bigG/14 → 32×32 个视觉特征
      └─ Position-Aware Adapter / Resampler
          ├─ 256 个可学习 Query
          └─ Cross-Attention 读取全部视觉特征
              └─ 256 个 LLM 维度的视觉向量
                  └─ 与文本 embedding 交错输入 Qwen-7B
                      └─ 自回归文本 / 坐标 token
```

#### 2.4.3 逐层维度流（从输入张量到输出张量）

以下只对应 2023 年初代 Qwen-VL 的公开配置。论文第一训练阶段使用较低分辨率，第二、三阶段把视觉输入提升到 `448×448`；下表描述高分辨率阶段。

```text
输入图像                  : [B, 3, 448, 448]
ViT-bigG/14 ×48           : [B, 1024, 1664]      # 32×32=1024 个 patch token
Resampler kv_proj         : [B, 1024, 4096]      # 1664→4096
可学习 Query              : [256, 4096]         # batch 维广播
Resampler Cross-Attention : [B, 256, 4096]
压缩视觉 token            : [B, 256, 4096]
文本 token                : [B, L, 4096]
拼接后序列                : [B, 256+L, 4096]
Qwen LLM 主干             : [B, 256+L, 4096]
输出 logits               : [B, 256+L, vocab_size]

```

#### 2.4.4 关键子模块详解

**A. Position-Aware Adapter（Cross-Attention Resampler）**

1. **模块作用**

    - 把高分辨率 ViT 产生的大量视觉 token 压缩成 LLM 能承受的固定长度视觉摘要。

2. **内部结构顺序**

    - 初始化 256 个可学习 Query

    - Query 作为 `Q`

    - ViT patch 特征作为 `K/V`

    - 对 Query 与视觉特征加入相应位置表示并做多头 Cross-Attention

    - 输出固定 256 个视觉 token

3. **矩阵/张量维度变化**

    - Query：`[B,256,4096]`

    - Key/Value 来源于视觉特征：`kv_proj` 先把 `[B,1024,1664]` 映射为 `[B,1024,4096]`，再送入 MultiheadAttention 的 K/V 投影

    - Resampler 使用 32 个头、每头 128 维：`Q=[B,32,256,128]`，`K,V=[B,32,1024,128]`

    - 注意力矩阵：`Q @ K^T → [B,32,256,1024]`

    - Softmax 后乘 `V` 并合并 32 个头：`[B,32,256,128] → [B,256,4096]`

**视觉特征如何转换为 K/V**

**第一步：ViT 输出原始视觉特征**

```text
输入图像 448×448×3
  ↓ ViT-bigG/14（48 层 Transformer，16 个 Self-Attention 头）
原始视觉特征：[B, 1024, 1664]
  （1024 = (448/14)² 个 patch，1664 = hidden size）

```

**第二步：Cross-Attention Adapter 把视觉特征压成固定长度，并转成 K/V**

```text
可学习查询参数 query          ：[256, 4096]          ← batch 维广播后作为 Q 的输入
视觉特征                      ：[B, 1024, 1664]
视觉特征 → kv_proj            ：[B, 1024, 4096]     ← Resampler 先统一到 embed_dim
MultiheadAttention 内部分头 Q ：[B, 32, 256, 128]
MultiheadAttention 内部分头 K/V：[B, 32, 1024, 128]

```

Cross-Attention 计算：

```text
Attention(Q, K, V) = Softmax(Q @ K.T / √d_h) @ V

```

维度流：

```text
Q @ K.T = [B, 32, 256, 128] @ [B, 32, 128, 1024]
        = [B, 32, 256, 1024]
Softmax 后乘 V[B, 32, 1024, 128] → [B, 32, 256, 128]
合并 32 个头                         → [B, 256, 4096]

```

**为什么 256 和 1024 不需要对应？**

Cross-Attention 天然支持 Q 与 K/V 序列长度不同：

- Q 的序列长度（256）决定**输出 token 数**，由 Q\_learnable 数量决定

- K/V 的序列长度（1024）是视觉素材库大小，由 ViT patch 数决定

- 两者的序列长度无须相等；进入 MultiheadAttention 前，通道维必须统一为 `embed_dim=4096`。官方实现正是用 `kv_proj: 1664→4096` 完成这一步。

直觉类比：256 个查询是 256 个“问题”，1024 个 patch 是“素材库”。每个问题去问所有 1024 条素材“你有多相关”，再用相关性权重对素材内容做加权混合，最终每个问题得到一个综合回答——问题有多少（256），答案就有多少（256）。

**第三步：输出拼入 LLM token 序列**

```text
视觉 token：[B, 256, 4096]
文本 token：[B, L, 4096]
  ↓ 拼接
[B, 256+L, 4096] → 输入 Qwen-7B LLM

```

4. **为什么这样设计**

    - 高分辨率图像带来更丰富细节，但 token 过多会压垮 LLM；固定 Query 压缩，是在"信息量"与"上下文预算"之间做平衡。

**B. 高分辨率视觉编码器**

1. **模块作用**

    - 保留更多局部细节，提升 OCR、检测、定位能力。

2. **内部结构顺序**

    - 高分辨率输入 → 更强 ViT 编码 → 输出更长、更细的 patch 序列。

3. **矩阵/张量维度变化**

    - `[B,3,448,448] → [B,1024,1664]`

4. **为什么这样设计**

    - 对初学者最直观的理解是：224 像拿手机远远看海报，448 像走近两步，字和边框都会更清楚。

#### 2.4.5 训练方式与目标函数

初代 Qwen-VL 的训练分三阶段，不能简化成一次“多模态语言建模”：

1. **视觉-语言预训练**：使用大规模图文对，只优化视觉编码器和适配器，LLM 冻结；先建立基础图文对应关系。
2. **多任务预训练**：解冻全部参数，提高视觉分辨率，并混合 caption、VQA、OCR、grounding 与图文交错数据；模型开始同时学习识别、定位和语言生成。
3. **监督微调**：冻结视觉编码器，使用对话式多模态指令数据微调 LLM 与适配器，得到 Qwen-VL-Chat。

对需要监督的输出位置集合 $\mathcal{A}$，主目标仍可写为

$$
\mathcal{L}_{\text{MLLM}}
=-\sum_{t\in\mathcal{A}}
\log p_\theta(y_t\mid x_{\text{img}},x_{\text{text}},y_{<t}).
$$

OCR 与定位能力不只来自更高分辨率。初代 Qwen-VL 还把归一化后的二维边界框表示为可生成的坐标 token，并使用包含文本和框标注的数据进行训练。

#### 2.4.6 优势、局限与下一代模型动机

|维度|优点|局限 / 下一步动机|
|---|---|---|
|细粒度能力|更高分辨率、更强视觉编码器，OCR 和定位能力明显增强。|本质仍是"视觉外置 + 语言主干"，不是原生统一训练。|
|上下文预算|用固定 Query 压缩视觉 token，适合长对话和多图输入。|压缩本身仍可能损失一部分细节。|
|工业化落地|把多模态理解模型推向了中文、OCR、定位、复杂场景落地。|它仍然几乎只会理解，不负责图像生成。|

#### 2.4.7 一句话总结

**初代 Qwen-VL 的关键，是用高分辨率 ViT、256 Query Resampler 与坐标 token，在可控上下文预算下兼顾文字识别、定位和对话。**

本节只描述 2023 年公开的 Qwen-VL 架构。后续 Qwen2-VL、Qwen2.5-VL 与 Qwen2.5-Omni 的视觉编码、分辨率处理和音视频能力已有明显变化，不能把这里的固定 256 Query 与 448×448 配置直接套到整个 Qwen 系列。下面沿这条演进线继续展开。

### 2.5 Qwen2-VL

#### 2.5.1 要解决的问题（痛点 & 背景）

初代 Qwen-VL 把所有图片缩放到固定分辨率，再压成固定 256 个视觉 token。这便于控制成本，却有两个直接问题：小图可能被无谓放大，长图、文档和超高分辨率图片又会因统一缩放丢失细节；把视频当作若干彼此独立的图片，也难以显式表达时间位置。

Qwen2-VL 的核心目标因此变成：**让视觉 token 数随图像信息量变化，并让同一套位置编码同时描述文字、二维图像和三维视频。** 它用 Naive Dynamic Resolution 替代固定 448×448 输入，用 M-RoPE 替代纯一维位置表达，并统一图像与视频的视觉处理范式。

#### 2.5.2 总体网络架构图

```text
任意宽高图像 / 采样后视频帧
  └─ 动态缩放到 patch 对齐尺寸
      └─ 视觉 Transformer（时空 patch）
          └─ 2×2 Spatial Merge + MLP 投影
              └─ 可变长度视觉 token
                  └─ 与文本 token 交错输入 Qwen2 LLM
                      ├─ M-RoPE：时间 / 高度 / 宽度位置
                      └─ 自回归文本、坐标与结构化结果
```

这里已经不再使用初代 Qwen-VL 的 256 个可学习 Query Resampler。视觉序列保留多少 token，主要由输入缩放后的面积和空间合并倍率决定。

#### 2.5.3 逐层维度流（动态形状）

设动态缩放后的单张图像为 `H'×W'`，`H'`、`W'` 与 patch 网格对齐；视觉 patch 边长为 14，空间合并倍率为 2。为统一图像与视频接口，静态图像可在时间维复制为两帧，再由时间 patch 聚合。忽略首尾特殊 token，可写成：

```text
动态缩放图像              : [B, 3, H', W']
构造时间维后              : [B, 3, 2, H', W']
14×14 空间 patch 后        : [B, 2, H'/14, W'/14, d_v]
时间 patch 聚合后          : [B, H'/14, W'/14, d_v]
2×2 Spatial Merge 后       : [B, H'/28, W'/28, 4d_v]
MLP 投影后视觉序列         : [B, N_v, d_llm]
其中 N_v                  ≈ (H'/28) × (W'/28)
与文本交错后              : [B, N_v+L, d_llm]
LLM 输出                  : [B, N_v+L, vocab_size]
```

上式解释了“动态分辨率”的计算含义：视觉 token 不再恒为 256，而是近似与缩放后像素面积成正比。实际实现还会受最小/最大像素预算、批处理 padding、图像边界和视频采样策略影响。

#### 2.5.4 关键子模块详解

**A. Naive Dynamic Resolution**

- 每张图根据自身宽高比和像素规模缩放到可 patchify 的尺寸，不强行变成同一个正方形。
- 大图分配更多视觉 token 以保留文字和局部细节，小图使用更少 token，减少无效计算。
- 它并不意味着“原始分辨率完全不变”，而是在预设像素/token 预算内保留原始宽高比与相对信息密度。

**B. 2×2 Spatial Merge**

- ViT 先产生细网格特征，再把相邻 `2×2` 空间位置合并，并通过 MLP 映射到 LLM 隐藏维度。
- 合并使送入语言主干的序列长度约降为四分之一；与固定 Query 压缩相比，它保留了规则的二维邻接关系，但 token 预算变为可变。

**C. M-RoPE（Multimodal Rotary Position Embedding）**

- 将 RoPE 的通道维拆成时间 `t`、高度 `h`、宽度 `w` 三部分。
- 文本 token 的三组位置 ID 取相同的一维序号；图像 token 的时间 ID 固定，空间 ID 随网格行列变化；视频 token 同时改变时间和空间 ID。
- 因此同一个 LLM 可以在不另加二维绝对位置表的情况下区分“第几帧、第几行、第几列”。M-RoPE 解决的是位置建模，不等同于图文语义对齐；后者仍依赖训练数据和损失。

#### 2.5.5 训练方式与目标函数

Qwen2-VL 延续“视觉表征先对齐、再进行多模态联合训练与指令微调”的总体思路，但把动态分辨率图像、视频、OCR、文档、定位和多图数据纳入训练。语言侧仍以条件自回归损失为主：

$$
\mathcal{L}_{\text{Qwen2-VL}}
=-\sum_{t\in\mathcal{A}}
\log p_\theta(y_t\mid x_{\text{vision}},x_{\text{text}},y_{<t}).
$$

动态分辨率和 M-RoPE 本身不是额外的 loss：前者改变输入 token 化方式，后者改变注意力中的位置相位；OCR、定位与视频能力来自这些结构和相应监督数据的共同作用。

#### 2.5.6 优势、局限与下一代模型动机

|维度|优点|局限 / 下一步动机|
|---|---|---|
|图像细节|token 数随面积变化，长图、文档和高分辨率场景不再被固定 448×448 强行压平。|极大图像仍会产生很长的视觉序列，成本转移到 ViT 与 LLM 注意力。|
|图像/视频统一|同一视觉接口与 M-RoPE 可表达 1D 文本、2D 图像和 3D 视频。|视频采样仍可能遗漏短暂事件，位置编码也不能替代内容建模。|
|空间结构|规则网格合并比固定 Query 更直接地保留空间位置。|全局视觉自注意在动态大网格上计算昂贵，推动后续引入窗口注意力。|

#### 2.5.7 一句话总结

**Qwen2-VL 的关键，是用动态分辨率把“每张图固定 256 token”改成“按信息量分配 token”，再用 M-RoPE 统一文本、图像和视频的位置坐标。**

### 2.6 Qwen2.5-VL

#### 2.6.1 要解决的问题（痛点 & 背景）

Qwen2-VL 已能接受可变分辨率，但大网格上的视觉全局注意力开销仍高；视频时间位置更多表达帧的相对次序，不足以稳定回答“第几秒发生了什么”；归一化坐标也会弱化模型对图片真实尺寸和尺度的感知。

Qwen2.5-VL 在不放弃动态分辨率的前提下，重点强化三点：**更高效的原生动态分辨率 ViT、更明确的空间尺度、与真实时间对齐的视频位置。** 这使其能力从一般图文问答进一步扩展到文档结构化解析、精确框/点定位、小时级视频理解和视觉 Agent。

#### 2.6.2 总体网络架构图

```text
任意宽高图像 / 动态 FPS 视频
  └─ 原生动态分辨率 ViT（从头训练）
      ├─ 大多数层：Window Attention
      ├─ 少数层：Full Attention
      └─ RMSNorm + SwiGLU
          └─ 2×2 Spatial Merge + 投影
              └─ 可变长度视觉 token
                  └─ Qwen2.5 LLM
                      ├─ 改进的 M-RoPE / 绝对时间对齐
                      └─ 文本、框、点、表格/JSON 等结构化输出
```

#### 2.6.3 逐层维度流（代表性示意）

空间 patch 与 merge 后的长度关系仍可沿用 Qwen2-VL，但注意力计算从“每层都看全图”变为“局部层为主、少量全局层沟通”：

```text
视觉 patch 序列             : [B, N_patch, d_v]
Window Attention 层         : [B, N_patch, d_v]  # 主要在局部窗口内交互
Full Attention 层           : [B, N_patch, d_v]  # 跨窗口交换全局信息
2×2 Merge + Projector       : [B, N_v, d_llm]
文本 token                  : [B, L, d_llm]
交错多模态序列              : [B, N_v+L, d_llm]
```

若窗口最多覆盖 `8×8` 个视觉 patch，则单个局部窗口的注意力矩阵规模最多约为 `64×64`；相比对全部 `N_patch` 做 `N_patch×N_patch` 注意力，视觉侧计算更可控。少数 Full Attention 层用于补回跨窗口全局关系。具体层数和隐藏维度依模型规格而异，不应把 3B、7B、72B 配置混写为一个常数。

#### 2.6.4 关键子模块详解

**A. 原生动态分辨率 ViT**

- 视觉编码器从头经历 CLIP 阶段、视觉—语言对齐阶段和端到端阶段，不再只是把一个固定分辨率 ViT 直接改造成动态输入。
- 大多数层使用 Window Attention，仅在少数层进行 Full Attention；官方公开架构中有 4 个全局注意力层，窗口上限为 `8×8`，小于窗口的区域保持实际大小而不强制补齐。
- 视觉 block 改用与 LLM 更一致的 RMSNorm 和 SwiGLU，简化组件差异；这不表示视觉与语言共用同一套 block 参数。

**B. 空间尺度与非归一化坐标**

- 检测框和点可直接按图像实际尺寸尺度表达，而不是一律归一化到固定区间。
- 模型因而能同时学习“对象在哪里”和“这张图有多大”，更适合文档版面、GUI 元素定位与视觉 Agent 操作。
- 坐标 token 的精确语法属于模型输出协议；结构设计本身不能保证框一定正确，仍需 grounding 数据监督。

**C. 动态 FPS 与绝对时间编码**

- 训练时采用不同 FPS，使模型适应不同采样密度和播放节奏。
- 时间维 position ID 按真实时间间隔推进，而不是简单把相邻采样帧编号为 0、1、2；因此同样相隔两个 token，可以表示不同的实际秒数。
- 这为长视频中的秒级事件定位提供了坐标基础，但能否覆盖小时级视频仍取决于采样、token 上限和上下文长度。

#### 2.6.5 训练方式与目标函数

Qwen2.5-VL 的视觉侧训练可概括为三个衔接阶段：先用图文对比式目标训练原生动态分辨率 ViT，再进行视觉—语言对齐，最后与 Qwen2.5 语言主干端到端训练；后训练继续混合多任务指令数据。最终回答、坐标和结构化文本仍用自回归目标监督，视觉预训练阶段则还包含对比学习目标。

可把联合阶段抽象为：

$$
\mathcal{L}
=\lambda_{\text{AR}}\mathcal{L}_{\text{AR}}
+\lambda_{\text{vision}}\mathcal{L}_{\text{vision-pretrain}},
$$

但两个目标不一定在同一阶段、同一批次同时优化；该式只用于区分“视觉表征学习”和“条件文本生成”两类监督，不能据此臆造官方混合权重。

#### 2.6.6 优势、局限与下一代模型动机

|维度|优点|局限 / 下一步动机|
|---|---|---|
|视觉效率|窗口注意力降低大动态网格在 ViT 侧的计算量，少数全局层维持跨区域交互。|视觉 token 进入 LLM 后仍占上下文和 KV Cache；超长视频成本依然显著。|
|空间理解|实际尺度坐标、框/点定位和文档解析更适合 GUI、表格与图表任务。|输出坐标是语言生成结果，可能出现格式正确但定位错误。|
|时间理解|动态 FPS 与绝对时间编码支持长视频理解和秒级事件定位。|只有视觉轨道，无法利用原始音轨中的语音、音乐和环境声。|
|Agent 能力|可根据屏幕图像定位元素并生成操作建议。|“看见并输出动作”不等于具备安全、可靠的闭环执行系统。|

#### 2.6.7 一句话总结

**Qwen2.5-VL 在 Qwen2-VL 的动态分辨率与 M-RoPE 基础上，用窗口化原生 ViT、真实空间尺度和绝对时间推进，把能力重心推向高效细粒度感知、长视频定位与视觉 Agent。**

### 2.7 Qwen2.5-Omni：从视觉语言到全模态

Qwen2.5-Omni 不是 Qwen2.5-VL 的简单“加一个语音输出头”。在视觉侧，它沿用 Qwen2.5-VL 的视觉编码思路：patch size 为 14，合并相邻 `2×2` 空间 token，并用动态分辨率处理图像、动态帧率处理视频；单张图片按两个相同帧进入统一图像/视频接口。更大的变化发生在视觉链路之外：

```text
Qwen2.5-VL 路线：图像/视频 ── 动态视觉编码 ── LLM ── 文本

Qwen2.5-Omni：图像/视频 ── 分块视觉编码 ─┐
             音频 ──────── 分块音频编码 ─┼─ 时间交错 + TMRoPE ── Thinker ── 文本
             文本 ───────────────────────┘                         └─ Talker ── 流式语音
```

- **输入扩展**：从文本、图像、视频扩展到文本、图像、视频、音频；音视频按时间块组织。
- **位置编码扩展**：从 Qwen2.5-VL 的多模态位置与绝对视频时间进一步发展为 TMRoPE，使音频帧和视频帧共享可对齐的时间尺度。
- **输出扩展**：Thinker 自回归生成文本，Talker 读取 Thinker 隐状态并生成离散语音 token，再由滑窗 DiT 与声码器流式还原波形。
- **边界**：公开的 Qwen2.5-Omni 输出文本和语音，不生成图像或视频，因此“Omni”描述的是多模态感知与语音交互范围，而不是任意模态生成。

Qwen2.5-Omni 的 Thinker-Talker、音视频时间交错、TMRoPE、训练阶段与流式语音解码在 **5.1 节**完整拆解。把它放在 Qwen-VL 演进线上看，关键转折是：视觉不再只是给文本回答提供上下文，而是与音频一起成为实时交互系统的同步感知流。

---

## 三、生成侧核心架构

> 从本章开始，主线切换到"**机器如何创造内容**"。如果说理解侧更关心"这是什么"，那生成侧关心的就是"每个像素该长什么样、每一帧该怎么动"。
>
>

### 3.1 生成系统总览

#### 3.1.1 要解决的问题（痛点 & 背景）

生成模型面对的难题和理解模型完全不同。理解模型只要提取高层语义，能回答"这是一只猫"就够了；但生成模型必须真的构造出**每一个像素、每一帧细节、每一处纹理和光影**。如果一步到位直接生成整张图，训练往往不稳定；如果在像素空间直接迭代，又会非常昂贵。

潜空间扩散的一条主流链路是：**自编码器负责有损压缩与解码，扩散/score 或 flow 目标定义训练与采样，U-Net/DiT 作为可学习网络预测噪声、速度或其他参数化目标。**这不是所有生成模型的唯一结构；像素扩散、自回归图像 token 与 GAN 仍是不同路线。

#### 3.1.2 总体网络架构图

```text
训练：
图像 x ── Autoencoder Encoder ── z0 ── 随机时间 t + 噪声 ε ── zt
                                                          │
文本 ── 文本编码器 ── 条件 c ──────────────────────────────┤
                                                          └─ 去噪网络 → 训练目标

采样：
随机噪声 zT + 条件 c ── 采样器多步更新 ── z0 ── Autoencoder Decoder ── 图像
```

VAE/Autoencoder、去噪网络和采样器是不同层次：前者定义像素与 latent 的双向映射，去噪网络学习随时间变化的预测量，采样器把该预测代入数值更新公式。

#### 3.1.3 逐层维度流（从输入张量到输出张量）

以 Stable Diffusion 风格的图像生成链路为例：

```text
文本提示词 prompt         : [B, 77]
文本编码器输出            : [B, 77, 768]
随机高斯噪声 z_T          : [B, 4, 64, 64]
去噪网络输入              : (z_t, t, text_cond)
多步迭代去噪              : [B, 4, 64, 64] -> [B, 4, 64, 64]
最终干净 latent z_0       : [B, 4, 64, 64]
VAE Decoder 输出图像      : [B, 3, 512, 512]

```

#### 3.1.4 关键子模块详解

**A. VAE：压缩与解压**

1. **模块作用**

    - 把高分辨率图片压到低分辨率潜空间，让扩散计算变便宜。

2. **内部结构顺序**

    - Encoder 下采样 → 采样 latent → Decoder 上采样。

3. **维度变化**

    - `[B,3,512,512] → [B,4,64,64] → [B,3,512,512]`

4. **为什么这样设计**

    - 空间位置从 $512^2$ 降到 $64^2$，减少 64 倍；但通道数、网络宽度、Attention 与实现都会影响实际 FLOPs 和显存，不能把端到端加速固定写成 64 倍。

**B. Diffusion：定义加噪与去噪规则**

1. **模块作用**

    - 规定训练时如何把干净样本逐步加噪，以及采样时如何一步步还原。

2. **内部结构顺序**

    - 正向加噪 → 训练网络预测噪声 → 反向采样还原。

3. **维度变化**

    - 形状通常保持不变：`[B,4,64,64]`

4. **为什么这样设计**

    - 把"画整张图"拆成很多个"擦掉一点噪声"的小问题，训练更稳定。

**C. 去噪网络：U-Net / DiT**

1. **模块作用**

    - 按训练参数化预测噪声 $\epsilon$、干净样本 $x_0$、速度 $v$、score 或 flow velocity，供采样器更新 latent。

2. **内部结构顺序**

    - 输入噪声 latent + 时间步 + 条件 → 输出噪声预测。

3. **维度变化**

    - 通常输入输出同形状：`[B,4,64,64] → [B,4,64,64]`

4. **为什么这样设计**

    - 采样器只负责按规则更新，真正学知识的是去噪网络。

#### 3.1.5 训练方式与目标函数

生成系统常见训练逻辑是：

- 先预训练或单独训练 VAE

- 固定 VAE，在潜空间上训练去噪网络

- 让网络预测噪声、速度或干净样本

经典噪声预测目标：

$$
\mathcal{L}_{\epsilon}
=
\mathbb{E}_{x_0,\epsilon,t}
\left[
\lVert\epsilon-\epsilon_\theta(x_t,t,c)\rVert_2^2
\right].
$$

#### 3.1.6 优势、局限与下一代模型动机

- **优势**：训练稳定、画质高、容易加条件控制。

- **局限**：采样步数多，推理慢；U-Net 的扩展性有限；高分辨率和视频生成成本很高。

- **下一代动机**：因此才有 DiT、Flow Matching、时空 Transformer、统一生成-理解架构等后续演化。

#### 3.1.7 一句话总结

**潜空间扩散可以理解成三件套：先做有损压缩，再在 latent 分布上学习反向生成过程，最后解码回图像。**

### 3.2 VAE

#### 3.2.1 要解决的问题（痛点 & 背景）

如果直接在原始像素空间做扩散，模型每一步都要处理超大张量，成本非常高。VAE 解决的问题就是：**能不能先把一张大图压缩成一个更小、但仍然可还原的潜变量，再在这个小空间里做生成？**

对**潜空间扩散**而言，自编码器是定义 latent 数据分布的基础模块；但像素空间扩散并不要求 VAE，因此不能把它说成所有扩散模型都不可缺少。

#### 3.2.2 总体网络架构图

```text
                         ┌─ μ(x) ─┐
图像 x ── Encoder ───────┤        ├─ 重参数化 z = μ + σ⊙ε ── Decoder ── 重建图像 x̂
                         └─ logσ² ┘                    ε ~ N(0,I)
```

标准 VAE 通过概率编码器 $q_\phi(z\mid x)$ 和生成模型 $p_\psi(x\mid z)$ 最大化证据下界；潜空间扩散常用的 AutoencoderKL 沿用高斯后验与 KL 正则，但还会加入感知/对抗重建目标，因此不应把工程实现等同于最简教科书 VAE。

#### 3.2.3 逐层维度流（从输入张量到输出张量）

以 Stable Diffusion 风格的 VAE 为例，输入为 `512×512` RGB 图像。

```text
输入图像                  : [B, 3, 512, 512]
Conv in                   : [B, 128, 512, 512]
Downsample 1              : [B, 128, 256, 256]
Downsample 2              : [B, 256, 128, 128]
Downsample 3              : [B, 512, 64, 64]
Mid Block                 : [B, 512, 64, 64]
输出分布参数              : [B, 8, 64, 64]     # 前4通道 μ，后4通道 logσ²
重参数化采样 z            : [B, 4, 64, 64]
Decoder Conv in           : [B, 512, 64, 64]
Upsample 1                : [B, 512, 128, 128]
Upsample 2                : [B, 256, 256, 256]
Upsample 3                : [B, 128, 512, 512]
输出图像                  : [B, 3, 512, 512]

```

#### 3.2.4 关键子模块详解

**A. Downsample**

1. **模块作用**

    - 逐步缩小空间分辨率，把图像信息压到更紧凑的特征图里。

2. **内部结构顺序**

    - `Conv 3×3, stride=2, padding=1`，通常与前后的 ResNet Block 配合使用。

3. **矩阵/张量维度变化**

    - 典型例子：`[B,128,512,512] → [B,256,256,256]`

    - 再下一层：`[B,256,256,256] → [B,512,128,128]`

4. **为什么这样设计**

    - 空间缩半，相当于让后续网络在更大感受野上看图；同时通道增加，是在用"通道深度"补偿损失的空间细节。

**B. Upsample**

1. **模块作用**

    - 把潜空间特征逐步恢复到高分辨率图像空间。

2. **内部结构顺序**

    - `nearest/bilinear interpolation → Conv 3×3`

3. **矩阵/张量维度变化**

    - 典型例子：`[B,512,64,64] → [B,512,128,128]`

    - 再经过卷积/残差块可进一步调通道：`[B,512,128,128] → [B,256,256,256]`

4. **为什么这样设计**

    - 相比反卷积，插值后再卷积更不容易产生 **checkerboard artifact（棋盘格伪影）**。

**C. ResNet Block**

1. **模块作用**

    - 在不破坏主干信息流的前提下，逐层提炼局部纹理和语义特征。

2. **内部结构顺序**

    - `GroupNorm → SiLU → Conv3×3 → GroupNorm → SiLU → Conv3×3 → + shortcut`

    - 若输入输出通道不同，shortcut 走 `1×1 Conv` 做投影。

3. **矩阵/张量维度变化**

    - 同通道示例：`[B,256,128,128] → [B,256,128,128]`

    - 异通道示例：主支路 `[B,128,256,256] → [B,256,256,256]`，shortcut 也映射成 `[B,256,256,256]` 后相加。

4. **为什么这样设计**

    - 残差连接让深层卷积网络更容易训练，也能避免过度破坏输入特征。

**D. Mid Self-Attention**

1. **模块作用**

    - 在最低分辨率层补全全局依赖，让远距离区域也能交换信息。

2. **内部结构顺序**

    - 特征图归一化 → `reshape [B,C,H,W]→[B,HW,C]` → Self-Attention → reshape 回 `[B,C,H,W]`

3. **矩阵/张量维度变化**

    - 以中间层 `[B,512,64,64]` 为例：

    - reshape 后：`[B,4096,512]`

    - Attention 后：`[B,4096,512]`

    - reshape 回：`[B,512,64,64]`

4. **为什么这样设计**

    - 只在最低分辨率加 Attention，成本最低，但全局感受野最大；如果在 512×512 全分辨率上做，自注意力成本会爆炸。

#### 3.2.5 训练方式与目标函数

VAE 训练同时优化两件事：

1. **重构项**：提高给定 $z$ 时还原 $x$ 的似然；
2. **KL 正则**：约束编码后验不要任意偏离先验。

从

$$
\log p_\psi(x)
\ge
\mathbb{E}_{q_\phi(z\mid x)}
\left[\log p_\psi(x\mid z)\right]
-
D_{\mathrm{KL}}
\left(q_\phi(z\mid x)\,\Vert\,p(z)\right)
$$

得到最小化形式

$$
\mathcal{L}_{\text{VAE}}
=
\mathcal{L}_{\text{recon}}
+
\beta D_{\mathrm{KL}}
\left(q_\phi(z\mid x)\,\Vert\,p(z)\right).
$$

若编码器输出对角高斯的 $\mu$ 和 $\log\sigma^2$，重参数化为

$$
\epsilon\sim\mathcal{N}(0,I),
\qquad
z=\mu+\sigma\odot\epsilon.
$$

随机性被移到与参数无关的 $\epsilon$ 上，因此梯度可以穿过 $\mu$ 与 $\sigma$。

用于潜空间扩散的 AutoencoderKL 往往还结合感知损失，部分实现加入对抗损失；它通常只施加较弱 KL 正则以兼顾重建。KL 不能“保证”无空洞，也不意味着直接从标准高斯采样该 VAE 就能得到高质量图像——真正的 latent 数据分布由后续扩散模型学习。

#### 3.2.6 优势、局限与下一代模型动机

|维度|优点|局限 / 下一步动机|
|---|---|---|
|计算效率|把扩散从像素空间搬到潜空间，成本大幅下降。|压缩不可避免会带来一定细节损失。|
|连续表示|避免 VQ 离散量化误差，并可与连续扩散目标衔接。|KL 太强可能损害重建，太弱会使 latent 尺度与分布更难控制。|
|系统角色|是潜空间扩散能落地的前提。|未来在视频场景中还需要扩展成 3D VAE 压缩时空维。|

#### 3.2.7 一句话总结

**VAE 做的事很朴素但极关键：把大图压成小 latent，让后面的扩散模型终于算得动。**

### 3.3 Diffusion 数学原理

#### 3.3.1 要解决的问题（痛点 & 背景）

有了 VAE，潜空间已经压缩好了，但**如何在这个空间里"造图"**仍然是难题。早期 GAN 试图一步到位生成整张图，训练极不稳定。Diffusion 模型的核心思想是：

> 把"生成整张图片"这个困难任务拆成很多步，每一步只做一件简单的事 —— **擦掉一点噪声。**
>
>

#### 3.3.2 正向加噪：一个不需要学习的"破坏"规则

给定干净 latent $x_0$，DDPM 定义从 $x_0$ 到 $x_T$ 的固定马尔可夫链。令 $\alpha_t=1-\beta_t$：

$$
q(x_t\mid x_{t-1})
=
\mathcal{N}
\left(
\sqrt{\alpha_t}\,x_{t-1},
\beta_t I
\right).
$$

由于高斯分布对线性组合封闭，可以跳过中间步骤直接采样任意 $t$：

$$
\bar{\alpha}_t=\prod_{s=1}^{t}\alpha_s,
\qquad
q(x_t\mid x_0)
=
\mathcal{N}
\left(
\sqrt{\bar{\alpha}_t}\,x_0,
(1-\bar{\alpha}_t)I
\right),
$$

$$
x_t
=
\sqrt{\bar{\alpha}_t}\,x_0
+
\sqrt{1-\bar{\alpha}_t}\,\epsilon,
\qquad
\epsilon\sim\mathcal{N}(0,I).
$$

$\bar{\alpha}_t$ 表示到第 $t$ 步仍保留的总信号比例；它通常随 $t$ 下降。训练时正是借助闭式公式随机抽一个 $t$，无需真的逐步加噪。

**维度流**：

```text
干净 latent x_0          : [B, 4, 64, 64]
正向加噪 x_t             : [B, 4, 64, 64]      # 形状始终不变，只是噪声占比越来越大

```

#### 3.3.3 反向去噪：神经网络要学的"修复"本领

生成模型从标准高斯 $p(x_T)$ 出发，学习反向转移

$$
p_\theta(x_{t-1}\mid x_t,c)
=
\mathcal{N}
\left(
\mu_\theta(x_t,t,c),
\Sigma_\theta(x_t,t,c)
\right).
$$

网络可以直接参数化均值/方差，也可以预测 $\epsilon$、$x_0$ 或 $v$，再换算成反向转移所需的量。下面的真实后验用于推导训练目标；采样时并不知道真实 $x_0$。

真实后验在给定 $x_0$ 时仍是高斯：

$$
q(x_{t-1}\mid x_t,x_0)
=
\mathcal{N}
\left(
\tilde{\mu}_t(x_t,x_0),
\tilde{\beta}_t I
\right),
$$

$$
\tilde{\mu}_t
=
\frac{\sqrt{\bar{\alpha}_{t-1}}\beta_t}
{1-\bar{\alpha}_t}x_0
+
\frac{\sqrt{\alpha_t}(1-\bar{\alpha}_{t-1})}
{1-\bar{\alpha}_t}x_t,
\qquad
\tilde{\beta}_t
=
\frac{1-\bar{\alpha}_{t-1}}
{1-\bar{\alpha}_t}\beta_t.
$$

采样时没有真实 $x_0$，所以网络必须从 $x_t,t$ 与条件 $c$ 估计它。若网络预测噪声 $\epsilon_\theta$，则

$$
\hat{x}_0
=
\frac{x_t-\sqrt{1-\bar{\alpha}_t}\,
\epsilon_\theta(x_t,t,c)}
{\sqrt{\bar{\alpha}_t}}.
$$

把 $\hat{x}_0$ 代入 $\tilde{\mu}_t$，就得到一次从 $x_t$ 更新到更干净 $x_{t-1}$ 的均值。常见简化训练目标为

$$
\mathcal{L}_{\epsilon}
=
\mathbb{E}_{x_0,\epsilon,t}
\left[
w(t)
\lVert
\epsilon-\epsilon_\theta(x_t,t,c)
\rVert_2^2
\right],
$$

其中 $w(t)$ 是可选时间权重；许多实现采用常数或经过 SNR 调整的权重。这个 MSE 与 DDPM 变分下界的参数化有关，并非“任意选择一个回归任务”。

若把前向边缘分布写成 $x_t=\alpha_t^\star x_0+\sigma_t^\star\epsilon$，其中 $\alpha_t^\star=\sqrt{\bar{\alpha}_t}$、$\sigma_t^\star=\sqrt{1-\bar{\alpha}_t}$，则条件 score 与噪声预测满足

$$
\nabla_{x_t}\log q(x_t\mid x_0)
=
-\frac{\epsilon}{\sigma_t^\star}.
$$

因此训练 $\epsilon_\theta$ 也可以解释为在不同噪声尺度上学习 score。这里是条件于 $x_0$ 的解析关系；神经网络在只有 $x_t$ 时，通过数据平均逼近相应边缘分布的 score。

#### 3.3.4 预测目标与采样器

**同一扩散过程的常见参数化**

- $\epsilon$-prediction：预测本次加入的噪声；
- $x_0$-prediction：直接预测干净样本；
- $v$-prediction：预测信号与噪声的组合，例如

$$
v
=
\sqrt{\bar{\alpha}_t}\,\epsilon
-
\sqrt{1-\bar{\alpha}_t}\,x_0.
$$

令 $a_t=\sqrt{\bar{\alpha}_t}$、$b_t=\sqrt{1-\bar{\alpha}_t}$，则

$$
\hat{x}_0=a_t x_t-b_t\hat{v},
\qquad
\hat{\epsilon}=b_t x_t+a_t\hat{v}.
$$

因此三者可在已知噪声日程时互相换算，但数值尺度、时间步权重和训练稳定性不同。

**Classifier-Free Guidance（CFG）**

训练时以一定概率丢弃条件，使同一网络同时学会有条件和无条件预测。采样时组合两次输出：

$$
\epsilon_{\text{cfg}}
=
\epsilon_\theta(x_t,t,\varnothing)
+
s\left[
\epsilon_\theta(x_t,t,c)
-
\epsilon_\theta(x_t,t,\varnothing)
\right].
$$

$s=1$ 给出条件预测，$s>1$ 通常增强提示词服从度，但过大可能降低多样性并导致过饱和或结构伪影。上式以 $\epsilon$-prediction 为例；若模型预测 $v$、$x_0$ 或 flow velocity，应在该模型与采样器约定的输出空间中做一致组合，不能把结果误当成 $\epsilon$。CFG 是推理时引导方法，不是 Cross-Attention 本身。

**采样器与训练目标不是同一概念**

DDPM、DDIM、DPM-Solver 等采样器决定如何把网络预测代入离散更新或 ODE/SDE 数值求解。改变采样器可能减少步数，但必须匹配模型的预测参数化、时间坐标和噪声日程。

**Flow Matching 与 Rectified Flow**

Flow Matching 直接回归一条概率路径对应的速度场，再通过常微分方程输运样本：

$$
\frac{\mathrm{d}x}{\mathrm{d}t}=u_\theta(x,t,c).
$$

以“$t=0$ 为噪声、$t=1$ 为数据”的独立端点耦合和直线路径为例，对一对训练端点有

$$
x_t=(1-t)x_{\text{noise}}+t x_{\text{data}},
\qquad
u_t(x_t\mid x_{\text{noise}},x_{\text{data}})
=x_{\text{data}}-x_{\text{noise}},
$$

$$
\mathcal{L}_{\text{flow}}
=
\mathbb{E}
\left[
\lVert
u_\theta(x_t,t,c)
-
(x_{\text{data}}-x_{\text{noise}})
\rVert_2^2
\right],
$$

同一个 $x_t$ 可能由多对端点产生，所以 MSE 的最优网络不是记住某一条配对速度，而是学习条件平均

$$
u^\star(x,t,c)
=
\mathbb{E}
\left[
x_{\text{data}}-x_{\text{noise}}
\mid x_t=x,c
\right].
$$

Rectified Flow 常采用上述直线插值；进一步的 reflow 会用已学习流产生新的端点耦合，使轨迹更直、低步数积分更容易。不同论文也可能把 $t=0$ 与 $t=1$ 的数据/噪声方向对调，此时速度符号和积分方向会一同改变。

它与 score-based diffusion 可在更一般的连续时间框架下联系起来，但路径、目标和采样方程不同，不能把 flow velocity 直接塞进 DDPM 的 $\epsilon$ 更新公式。

#### 3.3.5 优势、局限与下一代模型动机

- **优势**：训练稳定；容易引入条件控制；画质高。

- **局限**：步数多，推理慢；与 U-Net 结合时扩展性受限。

- **下一代动机**：引入 Transformer（DiT）、Flow Matching、Rectified Flow 等，使去噪网络更易扩展，采样更高效。

#### 3.3.6 一句话总结

**Diffusion 的本质，是先定义可控的前向扰动，再学习足以构造反向生成过程的网络；在连续时间视角下，它可解释为从噪声分布走向数据分布的动力系统。**

### 3.4 U-Net

#### 3.4.1 要解决的问题（痛点 & 背景）

在扩散里，去噪网络要同时看清**全局结构**（这是一只猫还是一条狗）和**局部细节**（毛发纹理、光影纹路）。U-Net 就是为此设计的：

> 先一路下采样抓全局，再一路上采样补细节，中间用 **Skip Connection** 把细节抄近路传回来。
>
>

#### 3.4.2 总体网络架构图

```text
zt ── Conv ── Down Block ── Down Block ── Mid Block ── Up Block ── Up Block ── Conv ── 预测量
                 │              │                         ▲            ▲
                 └──────── skip ┼─────────────────────────┘            │
                                └────────────────── skip ───────────────┘

每个尺度的 ResBlock ← 时间嵌入 t
带条件的 Attention  ← 文本/其它条件 c
```

“U”形结构描述的是多尺度信息流，不等于一种固定层数或固定通道表。不同 Stable Diffusion 版本会改变注意力所在尺度、通道数、ResBlock 数和预测头。

#### 3.4.3 逐层维度流（从输入张量到输出张量）

以下用 Stable Diffusion v1 风格通道作简化骨架。真实实现会在每个尺度保存多个 skip，并在上采样块逐个取出，所以不能把每个尺度只理解成一次拼接。

```text
输入噪声 latent          : [B, 4, 64, 64]
时间步 t                  : 标量 → [B, 1280]
文本 embedding            : [B, 77, 768]
Conv in                   : [B, 320, 64, 64]
Down @ 64×64             : [B, 320, 64, 64]  → 保存 skip → 下采样
Down @ 32×32             : [B, 640, 32, 32]  → 保存 skip → 下采样
Down @ 16×16             : [B, 1280, 16, 16] → 保存 skip → 下采样
Down @ 8×8               : [B, 1280, 8, 8]
Mid                       : [B, 1280, 8, 8]
Up @ 8→16                : 与对应 skip concat → ResBlock/Attention → [B, 1280, 16, 16]
Up @ 16→32               : 与对应 skip concat → ResBlock/Attention → [B, 640, 32, 32]
Up @ 32→64               : 与对应 skip concat → ResBlock/Attention → [B, 320, 64, 64]
Conv out                  : [B, 4, 64, 64]

```

#### 3.4.4 关键子模块详解

**A. ResNet Block（含时间步注入）**

1. **模块作用**

    - 在每个尺度上精炼特征，并注入当前时间步信息，让网络知道噪声强度。

2. **内部结构顺序**

    - 输入特征 `h`，时间嵌入 `t_emb`

    - `h → GroupNorm → SiLU → Conv3×3`

    - `t_emb → Linear → reshape 成 [B,C,1,1]` 加到中间特征上

    - 再 `GroupNorm → SiLU → Conv3×3`

    - 与 shortcut 相加

3. **矩阵/张量维度变化**（以通道 320 为例）

    - 特征：`[B,320,H,W]`

    - 时间嵌入：`[B,1280] → Linear → [B,320] → [B,320,1,1]`

    - 中间特征：`[B,320,H,W] + [B,320,1,1]`

4. **为什么这样设计**

    - 时间步影响的是"这一层应该去多少噪"，自然应该作为一种全局偏置注入到特征通道上。

**B. Cross-Attention**

1. **模块作用**

    - 让图像特征在去噪时能够"听从"文本提示词的指挥。

2. **内部结构顺序**

    - 把当前图像特征 `h` 投影成 `Q`

    - 文本 embedding 投影成 `K,V`

    - 计算 `Attention(Q_img, K_txt, V_txt)`

3. **矩阵/张量维度变化**（简化成单头示意）

    - 图像特征 flatten：`[B,C,H,W] → [B,HW,C]`

    - 文本特征：`[B,77,768]`

    - `Q=[B,HW,d]`，`K,V=[B,77,d]`

    - `QK^T → [B,HW,77]`

    - Softmax 后乘 `V`：`[B,HW,d]` → reshape 回 `[B,C,H,W]`

4. **为什么这样设计**

    - Query 来自图像，Key/Value 来自文本，含义就是：图像每个位置主动去"问"提示词：哪几个词和我最相关？

**C. Downsample / Upsample**

1. **模块作用**

    - 下采样用来扩大感受野，上采样用来恢复细节。

2. **内部结构顺序**

    - 下采样：一般是 `Conv 3×3, stride=2`，有时配合 AvgPool

    - 上采样：`nearest/bilinear 插值 → Conv 3×3`

3. **矩阵/张量维度变化**

    - 下采样示例：`[B,320,64,64] → [B,320,32,32]`

    - 上采样示例：`[B,640,16,16] → [B,640,32,32]`

4. **为什么这样设计**

    - 下采样压缩空间、增强全局视野；上采样用插值 + 卷积减少棋盘格伪影。

**D. Skip Connection（跳连，concat 还是 add？）**

1. **模块作用**

    - 把下采样阶段保留的细节特征直接抄近路送到对应的上采样阶段。

2. **内部结构顺序**

    - 以一对对称层为例：

    - 下行输出 `h_down` 保存起来

    - 上行时将 `h_up` 与 `h_down` 在通道维进行 concat

    - 再送入 ResNet Block 做融合

3. **矩阵/张量维度变化**

    - 例如在 32×32 尺度：

    - 下行输出：`[B,640,32,32]`

    - 上行当前：`[B,640,32,32]`

    - 拼接后：`[B,1280,32,32]`

4. **为什么这样设计**

    - 使用 concat 而非 add，是为了显式保留两路信息，并让后续卷积自由决定如何混合；通道翻倍也增加了表达能力。

#### 3.4.5 训练方式与目标函数

U-Net 是网络骨干，不绑定某一种目标。以 $\epsilon$-prediction 为例：

$$
\mathcal{L}_{\epsilon}
=
\mathbb{E}_{x_0,\epsilon,t}
\left[
\lVert\epsilon-\epsilon_\theta(x_t,t,c)\rVert_2^2
\right].
$$

#### 3.4.6 优势、局限与下一代模型动机

- **优势**：

    - 多尺度结构 + 跳连，非常适合兼顾全局与局部。

    - 与卷积搭配，对图像任务效果稳定、工程实践成熟。

- **局限**：

    - 依赖卷积，扩展到超大模型时 scaling law 不如 Transformer 规整。

    - 卷积仍以局部计算为主；虽然 Stable Diffusion U-Net 会在多个尺度插入 Attention，但高分辨率全局注意力昂贵，多尺度拓扑和通道配置也不如纯 Transformer 规整。

- **下一代动机**：

    - 自然演化出完全用 Transformer 替代 U-Net 的 DiT 系列。

#### 3.4.7 一句话总结

**U-Net 像一条 U 型过山车：下坡时抓全局，上坡时补细节，中间靠跳连把细节一路护送回来。**

### 3.5 DiT

#### 3.5.1 要解决的问题（痛点 & 背景）

U-Net 在图像扩散里非常成功，但它终究是一套卷积范式。随着模型规模增大、视频场景拉长、全局一致性要求提高，大家开始追问：**既然语言模型都已经被 Transformer 统一，生成模型能不能也彻底 Transformer 化？**

DiT（Diffusion Transformer）给出的答案是：**把 latent 切成 patch token，让去噪网络本身也变成一个标准 Transformer。**

#### 3.5.2 总体网络架构图

```text
latent zt ── Patchify + Linear + 2D Position ── DiT Block × L ── Linear + Unpatchify ── 预测量
                                                    ▲
时间步 t ── embedding ─┐                            │
类别 y ── embedding ───┴─ 相加成条件 c ── adaLN-Zero 调制每一层
```

原始 DiT 是**类别条件** ImageNet 模型；文本条件 DiT 是后续生成系统的常见扩展，可能使用 Cross-Attention、联合序列或额外调制，不能倒写成原始论文的固定结构。

#### 3.5.3 逐层维度流（从输入张量到输出张量）

以 **DiT-XL/2** 为例：latent 尺寸 `32×32×4`，patch 大小 `2×2`，工作维度 `1152`，28 层 block，16 个头。

```text
输入 latent               : [B, 4, 32, 32]
Patchify (p=2)            : [B, 256, 16]        # (32/2)^2=256, 每块 2×2×4=16
线性投影 Embed            : [B, 256, 1152]
加固定 2D sin-cos 位置编码 : [B, 256, 1152]
时间步 embedding          : [B, 1152]
类别条件 embedding         : [B, 1152]
融合条件 c_total          : [B, 1152]
DiT Block ×28             : [B, 256, 1152]
Final Linear              : [B, 256, 32]
Unpatchify                : [B, 8, 32, 32]      # learn_sigma=True：4 通道预测量 + 4 通道方差参数

```

#### 3.5.4 关键子模块详解

**A. Patchify / Unpatchify**

1. **模块作用**

    - 把二维 latent 图变成一维 token 序列，再在输出阶段折叠回二维空间。

2. **内部结构顺序**

    - Patchify：切 patch → flatten → linear projection

    - Unpatchify：线性投影回 patch 像素 → reshape → permute → 拼回二维网格

3. **矩阵/张量维度变化**

    - 输入：`[B,4,32,32]`

    - 切成 patch：`[B,16,16,2,2,4]`

    - 展平：`[B,256,16]`

    - 嵌入后：`[B,256,1152]`

    - 输出头：`[B,256,32]`

    - Unpatchify 后：`[B,8,32,32]`

4. **为什么这样设计**

    - Transformer 不擅长直接处理二维卷积网格，但非常擅长处理一维 token 序列。

**B. Self-Attention**

1. **模块作用**

    - 让每个 patch token 与所有其它 patch token 做全局交互。

2. **内部结构顺序**

    - `X → Q,K,V → 分头 → Q@K^T → Softmax → ×V → merge head → Linear`

3. **矩阵/张量维度变化**

    - 输入：`X=[B,256,1152]`

    - `Q,K,V=[B,256,1152]`

    - 分 16 头：`[B,16,256,72]`

    - 注意力矩阵：`[B,16,256,256]`

    - 与 `V` 相乘：`[B,16,256,72]`

    - 合并头：`[B,256,1152]`

4. **为什么这样设计**

    - 每一层每个 token 都能看到全图所有位置，这比卷积式局部感受野更天然地适合建模全局一致性。

**C. FFN**

1. **模块作用**

    - 在注意力完成 token 间通信后，对每个 token 做独立的通道混合与非线性变换。

2. **内部结构顺序**

    - `Linear 升维 4× → GELU → Linear 降维`

3. **矩阵/张量维度变化**

    - `[B,256,1152] → [B,256,4608] → [B,256,4608] → [B,256,1152]`

4. **为什么这样设计**

    - Attention 负责 token 间交互，FFN 负责每个 token 内部表示的非线性加工，两者分工明确。

**D. AdaLN / AdaLN-Zero**

1. **模块作用**

    - 把时间步、类别或文本条件注入每一层 Transformer block，而不必像 U-Net 那样大面积插入 Cross-Attention。

2. **内部结构顺序**

    - `c_total → Conditioning MLP → 输出 6 组参数 (γ1, β1, α1, γ2, β2, α2)`

    - 第一组调制 Attention 前 LayerNorm

    - 第二组调制 FFN 前 LayerNorm

    - `α1/α2` 作为残差门控

3. **矩阵/张量维度变化**

    - 条件输入：`[B,1152]`

    - MLP 输出：`[B,6912]`

    - 切分后得到 6 组 `[B,1152]`

    - 广播到 token 维：`[B,1,1152] → [B,256,1152]`

4. **为什么这样设计**

    - 这是一种更"轻巧但深入"的条件注入方式。与在主干里拼接 token 相比，它直接调制每一层的归一化与残差通路。

**AdaLN-Zero 为什么要零初始化？**

    - 生成调制参数的线性层以零初始化，使每个 Transformer block 的残差分支门控初始为 0，因此 block 初始近似恒等映射；

    - 最终预测层也使用零初始化，所以初始网络输出接近 0，而不是把输入原样当作噪声预测；

    - 训练随后逐步打开注意力、FFN 和条件调制通路。

#### 3.5.5 训练方式与目标函数

原始 DiT 是扩散模型的网络骨干，论文使用扩散目标；后续系统也把类似 Transformer 骨干用于 $v$ 预测或 Flow Matching。**DiT 描述的是网络结构，DDPM/Flow Matching 描述的是训练与采样框架**，二者不能当作同一层概念。区别于 U-Net 的核心在于：

- 去噪网络不再是卷积 U-Net，而是 Transformer block 堆叠；

- 条件注入不再依赖大量 Cross-Attention，而更偏向 adaLN 调制或联合 token 化。

#### 3.5.6 优势、局限与下一代模型动机

|维度|优点|局限 / 下一步动机|
|---|---|---|
|全局一致性|全局自注意力天然适合图像整体构图与视频长程一致性。|注意力成本随 token 数平方增长，超高分辨率仍然昂贵。|
|可扩展性|更符合 Transformer 的 scaling 规律，适合做超大模型。|对训练数据、训练技巧、算力预算要求更高。|
|统一潜力|更容易和语言、视频、统一 token 框架接轨。|仍需解决高效采样与跨模态统一建模问题。|

#### 3.5.7 一句话总结

**DiT 的核心转变，是把"去噪"这件事彻底 Transformer 化：latent 先切成 token，再让全局注意力去学会如何把噪声一步步擦掉。**

### 3.6 图像→视频扩展

#### 3.6.1 要解决的问题（痛点 & 背景）

图片生成只需要每一帧好看；视频生成还要求**前后帧连续、运动合理、长时依赖稳定**。换句话说，视频问题比图片多了一条最难的轴：**时间轴**。

#### 3.6.2 从图像生成到视频生成，系统哪里变了？

|模块|图像生成|视频生成扩展|
|---|---|---|
|VAE|2D VAE，只压空间维。|3D VAE，同时压缩时间和空间，latent 可能形如 `[B,C,T/4,H/8,W/8]`。|
|去噪网络|U-Net / DiT 面向二维 latent。|扩展为时空 U-Net 或时空 DiT，Attention 覆盖时间维。|
|训练目标|预测单张图像噪声。|预测时空块噪声，同时维持跨帧一致性。|

#### 3.6.3 三个新增核心难点

1. **时间一致性**：人物不能上一帧长这样，下一帧突然换脸。

2. **运动合理性**：动作轨迹和速度变化必须符合真实世界规律。

3. **计算爆炸**：token 数会随帧数暴涨，Attention 成本更快失控。

#### 3.6.4 为什么常见方案会采用时空压缩与时空建模？

因为视频生成的计算复杂度太高了。一个直观对比：

- 1024×1024 图像的 token 数约是 512×512 的 4 倍；

- Self-Attention 成本近似随 token 数平方增长，算力约变成 16 倍；

- 若不压缩并对所有时空 token 做联合全注意力，帧数从 1 增至 $T$ 会再让注意力项近似增加 $T^2$ 倍。

设空间 token 数为 $N_s=H'W'$，视频序列长度为 $N=T'N_s$，朴素联合注意力的矩阵大小为 $N^2=T'^2N_s^2$。分解式空间/时间注意力把计算拆成约 $T'N_s^2+N_sT'^2$，但也改变了信息交互路径。

因此许多潜空间视频生成系统会组合：

- 可用 **3D/时空 VAE** 同时压缩时间和空间，也可采用逐帧 VAE、因果视频 tokenizer 等变体；

- 再用时空 DiT、带 Temporal Attention 的 U-Net，或分解空间/时间计算的其他骨干；

- 在 token 数可承受的 latent 空间里做去噪。

#### 3.6.5 一句话总结

**视频生成并不是"把图片生成多跑几次"，而是在空间生成问题上，再叠加一整条时间建模问题。**

---

## 四、统一理解与生成

### 4.1 根本矛盾

#### 4.1.1 要解决的问题（痛点 & 背景）

到了这一步，真正的核心矛盾终于露出来了：

- **理解侧**希望拿到的是高层、连续、语义化的特征；

- **生成侧**希望拿到的是低层、可重构、细节充分的特征。

这不是一个简单的工程接口问题，而是**两类表征目标经常产生优化张力**的问题。

#### 4.1.2 总体结构图

```text
                         ┌─ 语义编码器 ── 连续高层特征 ── 理解/文本输出
同一张图像 ──────────────┤
                         └─ 重构编码器 ── 离散 code 或连续 latent ── 图像输出
                                      │
                     选择：统一表示、双路径，或在主干内部按目标分专家
```

#### 4.1.3 对照维度流（用同一张图看两条路径）

```text
理解侧（CLIP ViT）
输入图像                  : [B, 3, 224, 224]
Patch token               : [B, 196, 768]
编码后高层语义            : [B, 768] / [B, 196, 768]
特点                      : 连续、高层、偏语义

生成侧（VAE / VQ tokenizer）
输入图像                  : [B, 3, 512, 512]
压缩 latent / code        : [B, 4, 64, 64] 或 [B, 1024]
特点                      : 可重构、保细节、偏像素

```

#### 4.1.4 为什么这会变成统一架构的核心难题？

- 理解模型要的是"这是一只猫"；

- 生成模型要的是"这只猫每根毛发该落在哪个像素上"。

前者通常鼓励对视角、纹理等扰动保持一定不变性，后者必须保留足够信息以重建具体样本。这个张力不是绝对规律：OCR/定位需要细粒度理解，生成模型也必须掌握高层语义。

分析统一模型时，应分别看四个彼此独立的轴：

1. **输入表示是否统一**：共享 VQ tokenizer，还是理解编码器与生成 tokenizer/latent 分开；
2. **主干参数是否统一**：完全共享 Transformer，还是使用模态/目标专用 Expert；
3. **训练目标是否统一**：全部 next-token，还是同时存在交叉熵与 diffusion/flow MSE；
4. **注意力拓扑是否统一**：全序列严格因果、视觉块内双向的 block-causal，还是每个生成时间步内对带噪 latent 做双向更新。

因此“共享一个主干”不等于“共享一个 tokenizer”，“都在一个序列中交互”也不等于“使用同一组 Transformer 参数”或“具有相同的信息可见性”。例如 Chameleon 的离散图像 code 按 token 严格自回归，BAGEL 的视觉片段内部则可以双向注意，同时仍通过片段级广义因果掩码保持跨片段生成顺序。

#### 4.1.5 一句话总结

**统一理解与生成最难的地方，不是把模块拼起来，而是让"语义特征"和"细节特征"在同一个系统里和平共处。**

### 4.2 Chameleon

#### 4.2.1 要解决的问题（痛点 & 背景）

Chameleon 采用 token-based early fusion：图像由图像 tokenizer 离散化，文本由文本 tokenizer 编码，两类 ID 使用各自词表区间，但被交错进**同一个自回归 token 序列**，再由同一 Transformer 预测。统一的是序列建模与主干，不是把图像和文本强行交给同一个 tokenizer。

#### 4.2.2 总体网络架构图

```text
文本 ── 文本 tokenizer ── text IDs ─┐
                                     ├─ 图文交错的统一 ID 序列
图像 ── VQ tokenizer ── image IDs ──┘
                                     └─ 单一 Decoder-Only Transformer
                                         └─ 联合词表 next-token logits
                                             ├─ 文本 ID → 文本
                                             └─ 图像 ID → VQ Decoder → 图像
```

#### 4.2.3 逐层维度流（代表性示意）

Chameleon 论文中的图像 tokenizer 把一张 `512×512` 图片离散成 `1024` 个 code，图像码本大小为 `8192`；完整 SentencePiece/BPE 词表大小为 `65536`，其中包含这些图像 code。下面以 7B 级主干的 `4096` hidden size 说明维度；34B 版本宽度不同。

```text
输入文本 token             : [B, L]
输入图像                  : [B, 3, 512, 512]
VQ tokenizer 编码         : [B, 1024]          # 每个 ID 来自 8192 项图像码本
统一词嵌入                : [B, T, 4096]      # T 为图文交错后的总长度
Decoder-only LLM          : [B, T, 4096]
输出 logits               : [B, T, 65536]     # 统一词表包含文本与图像 code

```

#### 4.2.4 关键子模块详解

**A. 离散图像 tokenizer**

1. **模块作用**

    - 把图像转成和文本类似的离散 token，便于统一自回归建模。

2. **内部结构顺序**

    - 图像编码 → 量化到 codebook → 输出离散 token ID。

3. **矩阵/张量维度变化**

    - `[B,3,512,512] → [B,32,32,d] → [B,1024]`

4. **为什么这样设计**

    - 如果图文都变成 token，统一 decoder-only 是最自然的建模方式。

**B. 单一 Decoder-Only Backbone**

1. **模块作用**

    - 用同一套自回归 Transformer 同时处理文字和图像 token。

2. **内部结构顺序**

    - 图像 token 与文本 token 拼接 → 统一嵌入 → 自回归建模 → 预测下一个 token。

3. **矩阵/张量维度变化**

    - `[B,T] → [B,T,4096] → [B,T,4096]`

4. **为什么这样设计**

    - 结构最整齐，训练目标也最统一。

#### 4.2.5 训练方式与目标函数

Chameleon 使用统一自回归目标：

$$
\mathcal{L}_{\text{AR}}
=-\sum_{t\in\mathcal{S}}
\log p_\theta(x_t\mid x_{<t}),
$$

其中 $x_t$ 可以是文本 token，也可以是图像 token，$\mathcal{S}$ 是参与监督的位置。目标形式统一，不意味着两种 token 的统计分布、词表大小或解码误差相同。

混合模态词表的熵和 logit 尺度差异会使大规模训练更不稳定。Chameleon 在注意力内部对 $Q/K$ 做归一化，并对最终 softmax 的配分函数加入 z-loss。若位置 $t$ 的 logits 为 $z_{t,j}$，可写成

$$
\mathcal{L}
=
\mathcal{L}_{\text{AR}}
+
\lambda_z
\frac{1}{|\mathcal{S}|}
\sum_{t\in\mathcal{S}}
\left[
\log\sum_j\exp(z_{t,j})
\right]^2,
\qquad
\lambda_z=10^{-5}.
$$

QK-Norm 控制注意力 softmax 的输入范数，z-loss 控制输出 softmax 的整体 logit 漂移，两者解决的不是同一个位置。原论文还采用分阶段数据混合：前 80% 以大规模文本、图文对和图文交错数据为主，后 20% 降低原数据权重并加入更高质量数据。

#### 4.2.6 优势、局限与下一代模型动机

- **优势**：架构最统一、训练目标最干净、图文交错生成天然顺滑。

- **局限**：离散图像 tokenizer 以重构为重要目标，提供给主干的码本表示未必是最优的判别/语义特征；原论文也明确指出其 tokenizer 对大量文字图像的重建较弱。大图像词表与长图像序列同样增加建模成本。

- **下一代动机**：后续工作进一步探索为理解和生成使用不同视觉编码路径，同时仍共享推理主干。

#### 4.2.7 一句话总结

**Chameleon 的关键贡献是证明离散图像 token 与文本 token 可以在一个 early-fusion 自回归序列中交错理解和生成；代价是图像 tokenizer 的语义/重构取舍与较长序列成本。**

### 4.3 Janus

#### 4.3.1 要解决的问题（痛点 & 背景）

既然单一 tokenizer 同时兼顾语义和细节很困难，那更务实的办法就是：**保留两套最擅长各自任务的编码器，但让它们共享同一个 LLM 大脑。**

Janus 的名字恰好说明了这点：它像一个双面神，一面朝向理解，一面朝向生成。

#### 4.3.2 总体网络架构图

```text
理解图像 ── SigLIP ── Understanding Adaptor ─┐
文本 ── 文本 tokenizer / embedding ──────────┼─ 统一 AR Transformer
生成图像 ── VQ tokenizer ── Generation Adaptor ┘       ├─ 文本预测头
                                                       └─ 图像 code 预测头
```

#### 4.3.3 逐层维度流（代表性示意）

以下按初代 Janus 论文的公开配置书写：所有图像统一到 `384×384`，理解侧使用 SigLIP-Large-Patch16-384，生成侧 VQ tokenizer 的下采样因子为 16、码本大小为 16,384。用 $d_{\text{LLM}}$ 表示 DeepSeek-LLM-1.3B 的隐藏维度，避免把后续 Janus 版本配置误套进来。

```text
=== 理解分支 ===
输入图像                  : [B, 3, 384, 384]
SigLIP-L/16               : [B, 576, d_v]       # 24×24 网格，d_v 由视觉编码器决定
Understanding Adaptor     : [B, 576, d_LLM]     # 两层 MLP

=== 生成分支 ===
输入图像                  : [B, 3, 384, 384]
VQ tokenizer              : [B, 576]            # 24×24 个离散 code，范围 [0,16383]
Generation Adaptor        : [B, 576, d_LLM]     # codebook embedding 经两层 MLP

=== 共享 LLM Backbone ===
统一主干                  : [B, T, d_LLM]
输出                      : 文本词表 logits / 16384 维图像 code logits

```

#### 4.3.4 关键子模块详解

**A. 双编码器解耦**

1. **模块作用**

    - 理解侧保留语义最强的视觉编码器，生成侧保留细节最强的图像 tokenizer。

2. **内部结构顺序**

    - 理解分支：`图像 → SigLIP → Understanding Adaptor`

    - 生成分支：`图像 → VQ tokenizer → 图像 token embedding`

3. **矩阵/张量维度变化**

    - 理解：`[B,3,384,384] → [B,576,d_v] → [B,576,d_LLM]`

    - 生成：`[B,3,384,384] → [B,576] → [B,576,d_LLM]`

4. **为什么这样设计**

    - 不强行让同一个 tokenizer 同时干两种互相冲突的工作。

**B. 共享 LLM 主干**

1. **模块作用**

    - 让理解与生成共享推理能力和语言能力。

2. **内部结构顺序**

    - 不同模态表示被统一投喂给同一个 decoder-only backbone。

3. **矩阵/张量维度变化**

    - 两种路径最终都映射为长度可变、隐藏维度为 $d_{\text{LLM}}$ 的序列。

4. **为什么这样设计**

    - 把真正昂贵、真正通用的"推理大脑"共享起来，是在效果和结构复杂度之间做折中。

#### 4.3.5 训练方式与目标函数

初代 Janus 使用三个阶段：

1. **Adaptor 与图像预测头对齐**：冻结两套视觉编码器和 LLM，只训练两种 adaptor 与图像 head；
2. **统一预训练**：解冻 LLM，视觉编码器仍保持冻结，混合纯文本、多模态理解和图像生成数据；
3. **监督微调**：除生成编码器外联合微调，混合文本对话、视觉问答和视觉生成数据。

三类任务都采用交叉熵 next-token 目标，不为理解与生成另设 loss 权重：

$$
\mathcal{L}_{\text{Janus}}
=-\sum_{t\in\mathcal{S}}
\log p_\theta(x_t\mid x_{<t}).
$$

理解样本只在文本回答位置计算损失；视觉生成样本只在图像 code 序列上计算损失。

初代 Janus 也把 CFG 用在**离散自回归 logits** 上，而不是扩散噪声上。训练时以 10% 概率把文生图条件替换为 pad token，使模型学到无条件分支；推理时对每个图像 code 的 logits 组合

$$
\ell_{\text{cfg}}
=
\ell_u+s(\ell_c-\ell_u),
$$

再从组合后的离散分布中采样。它和扩散 CFG 共享“条件减无条件”的思想，但一个作用于 code logits，另一个通常作用于连续去噪/速度预测，不能混用采样公式。

#### 4.3.6 优势、局限与下一代模型动机

- **优势**：避开单一 tokenizer 的硬冲突，两边能力都比较稳。

- **局限**：初代 Janus 的图像生成是 384×384 离散 code 的串行自回归预测，长视觉序列会带来采样延迟与误差累积；两套编码/解码接口也仍需分别维护。

- **下一代动机**：后续路线探索更高分辨率 tokenizer、并行/连续生成目标，以及让主干内部进一步按理解与生成分配参数。

#### 4.3.7 一句话总结

**Janus 的精髓是：既然一个 tokenizer 很难兼顾语义和细节，那就让两套编码器各司其职，再共享同一个推理大脑。**

### 4.4 BAGEL

#### 4.4.1 要解决的问题（痛点 & 背景）

BAGEL 采用统一 Decoder-Only 框架，但不是“共享 QKV、只把 FFN 做成普通 MoE”。其公开实现使用 Mixture-of-Transformer-Experts（MoT）：理解 token 与生成 latent 选择不同的 Transformer 参数路径，同时在同一序列上进行全局交互。

核心问题是：如果所有模态都共享一个 decoder-only Transformer，如何既保持深度融合，又不让不同模态互相污染？

#### 4.4.2 总体网络架构图

```text
文本 token ───────────────┐
SigLIP2 / ViT token ──────┼─ 广义因果序列 ── Understanding Transformer Expert ── 文本 logits
                          │          │
FLUX VAE latent ── 加噪 ──┘          └─ Generation Transformer Expert ── rectified-flow velocity

路由：文本与 ViT token → 理解 Expert；VAE token → 生成 Expert
交互：不同 Expert 产生的 Q/K/V 仍按统一注意力掩码在同一上下文中计算注意力
```

#### 4.4.3 逐层维度流（代表性示意）

公开的 BAGEL 是 7B 激活参数、14B 总参数的 MoT。理解侧以固定 384 分辨率的 SigLIP2-so400m/14 初始化，通过位置编码插值把最大输入扩到 `980×980`，再用 NaViT 支持 native aspect ratio；生成侧使用冻结的 FLUX VAE，空间下采样 8 倍、latent 通道数 16，再经 `2×2` patch embedding。用符号维度可以避免把分辨率相关的 token 数写成固定常数：

```text
文本 token                 : [B, T_text, d]
理解视觉 token             : [B, T_vit, d]
图像 latent                : [B, 16, H/8, W/8]
2×2 patch embedding        : [B, T_vae, d], T_vae=(H/16)(W/16)
打包后的统一序列           : [B, T, d]
理解 Transformer Expert   : 处理文本与理解视觉特征
生成 Transformer Expert   : 处理带噪 VAE token
跨 token 信息交互          : 由广义因果注意力掩码决定
输出                       : 文本 logits / 图像 flow 预测

```

#### 4.4.4 关键子模块详解

**A. 同一序列上的跨模态注意力**

1. **模块作用**

    - 让理解 token、文本条件与生成 latent 在同一上下文中交换信息。

2. **内部结构顺序**

    - token 按模态切分为连续片段：片段通常可读取更早片段；文本片段内部使用因果注意力，视觉片段内部使用双向注意力。

    - 在普通图文交错生成中，一张历史图像同时保留 ViT token 与干净 VAE token，供后续片段读取；为当前 Rectified Flow 回归构造的带噪 VAE token 不作为普通后续上下文。推理时只缓存干净 VAE token 与 ViT token 的 K/V；一张图生成完毕后，其带噪表示会被干净表示替换。

    - 多图/视频训练中的 **diffusion forcing** 是一个重要例外：当前图像可以读取前序图像的带噪表示；连续图像还可以按组采用相同噪声等级，并在组内使用全注意力。也就是说，BAGEL 的“广义因果掩码”会随数据组织与训练任务变化，不能简化成一张固定的下三角掩码。

3. **矩阵/张量维度变化**

    - 各专家输入输出都保持 `[B,T_i,d]`，注意力连接规则决定不同 token 之间谁可读取谁。

4. **为什么这样设计**

    - “共享注意力上下文”不等于“所有 token 共用同一组 QKV 权重”。BAGEL 的 MoT 复制 Qwen2.5 block 的全部可训练参数，硬路由到两套完整 Expert；跨模态交互发生在统一注意力计算和掩码所允许的 K/V 上。普通交错训练强调与自回归推理一致的历史条件，多图/视频 diffusion forcing 则用带噪历史条件学习跨图像、跨帧依赖。

**B. Mixture-of-Transformer-Experts**

1. **模块作用**

    - 为理解与生成提供不同的完整 Transformer 变换，而不只是不同 FFN。

2. **内部结构顺序**

    - 根据 token/任务路径选择理解 Expert 或生成 Expert；这更接近确定性的模态/目标路由，不应类比为对每个 token 学习 Top-k 的通用稀疏 MoE。

    - 两套 Expert 都继承 Qwen2.5 block 的 RMSNorm、SwiGLU、RoPE 与 GQA，并在注意力中额外加入 QK-Norm；后者用于控制 $Q/K$ 的尺度，帮助稳定混合 AR 与连续生成目标的训练。

3. **矩阵/张量维度变化**

    - 两条路径维度兼容，但使用不同的 Attention、Norm 与 MLP 参数。

4. **为什么这样设计**

    - 同一序列保证交互，完整参数路径分工分别适配 next-token 理解/文本生成与连续图像生成。

**C. 位置与时间步注入**

1. **模块作用**

    - 区分视觉 token 的二维位置，并告诉生成 Expert 当前 VAE token 处于哪个噪声时间。

2. **内部结构顺序**

    - ViT/VAE token 加二维位置编码；

    - 带噪 VAE token 的初始 hidden state 再加 timestep embedding；

    - 进入按模态硬路由的 Transformer block。

3. **为什么这样设计**

    - BAGEL 没有沿用原始 DiT 的 adaLN 条件注入，而是把 timestep embedding 直接加到 VAE token 的初始隐藏状态。这个选择说明“使用 Rectified Flow”并不强制网络必须采用某一种 DiT 条件结构。

#### 4.4.5 训练方式与目标函数

BAGEL 不是只用一个统一的离散自回归 loss。文本/理解位置使用 next-token 交叉熵，带噪 VAE token 使用 Rectified Flow 的 MSE：

$$
\mathcal{L}_{\text{BAGEL}}
=
\lambda_{\text{CE}}\mathcal{L}_{\text{CE}}
+
\lambda_{\text{flow}}
\mathbb{E}
\left[
\lVert
u_\theta(x_t,t,c)-u^\star
\rVert_2^2
\right].
$$

公开训练配方在预训练、持续训练和 SFT 阶段采用 `CE : MSE = 0.25 : 1`。这个比例是**损失权重**，不是文本样本与图像样本的采样比例；后者另由数据混合配方控制。图像编辑和指令任务通过数据格式与对应的 CE/flow 监督进入训练，并没有报告一个独立的通用 $\mathcal{L}_{\text{task}}$ 项。整体训练分为 Connector Alignment、Pre-training、Continued Training 和 SFT 四阶段。

为支持交错图像推理时的 CFG，训练还会独立随机丢弃文本、ViT 和干净 VAE 条件，公开概率分别为 `0.1 / 0.5 / 0.1`。这与“把条件全部换成空提示”的简化教材写法相比更细：BAGEL 可以针对不同条件源构造无条件或弱条件分支，再在 flow 预测空间组合有条件与无条件输出。

#### 4.4.6 优势、局限与下一代模型动机

- **优势**：

    - 结构更统一；

    - 文本、视觉理解与连续图像生成可以共享上下文；

    - 更容易接上统一强化学习和统一后训练。

- **局限**：

    - 训练复杂度高；

    - 两套 Transformer 参数路径、混合目标和注意力掩码增加训练与部署复杂度。

- **下一代动机**：

    - 继续向音频、视频扩展，走向真正的 Omni。

#### 4.4.7 一句话总结

**BAGEL 的关键点，是在同一 Decoder-Only 上下文中用 MoT 分离理解与生成的完整 Transformer 参数路径，并联合 next-token 与连续图像生成目标。**

### 4.5 三路线对比表

|路线|代表模型|核心思路|最大优点|最大问题|
|---|---|---|---|---|
|离散 token early fusion|Chameleon|文本与图像分别 token 化后进入同一混合序列，由统一自回归主干建模。|图文交错输入输出自然，目标统一。|图像码本的语义/重构取舍和序列成本。|
|双视觉编码路径 + 统一 AR 主干|Janus|理解用 SigLIP 连续特征，生成用 VQ code；两者共享一个 AR Transformer。|避开单一视觉表示的语义/重构冲突，目标仍统一。|离散图像 code 串行采样成本高。|
|Decoder-only + MoT|BAGEL|理解/生成使用不同 Transformer Expert，在同一序列上下文中交互，并联合 AR 与 flow 目标。|兼顾连续图像生成和语义理解。|参数路径、掩码与混合训练复杂。|

---

## 五、全模态与未来

### 5.1 Qwen2.5-Omni

#### 5.1.1 要解决的问题（痛点 & 背景）

到了 Omni 阶段，图像和文本已经不够了。真正贴近人的交互，必须同时处理：

- 文本

- 图像

- 音频

- 视频

而且不仅要理解，还要实时输出文本或语音。这里具体指 **Qwen2.5-Omni**：它接收文本、图像、音频和视频，并以流式方式同时生成文本与自然语音；公开报告没有把图像/视频生成列为输出能力。其核心是 Thinker + Talker。

#### 5.1.2 总体网络架构图

```text
文本 ─────────────────────┐
图像/视频 ── 视觉编码器 ──┼─ 时间交错 + TMRoPE ── Thinker ── 文本 token
音频 ─────── 音频编码器 ──┘                          │
                                                    └─ 隐状态 + 已生成文本
                                                        └─ dual-track Talker
                                                            └─ 离散语音 token
                                                                └─ sliding-window Flow-Matching DiT
                                                                    └─ mel-spectrogram
                                                                        └─ modified BigVGAN
                                                                            └─ 流式波形
```

#### 5.1.3 逐层维度流（代表性示意）

图像分辨率、视频帧数和音频时长都会改变 token 数；Qwen2.5-Omni-7B 的隐藏维度也不是所有 Omni 模型的通用常数。因此以下只标接口关系：

```text
文本 token                : [B, T_text, d]
图像 / 视频特征            : [B, T_vision, d]
音频帧级特征               : [B, T_audio, d]
时间交错后的 Thinker 输入   : [B, T_total, d]
Thinker 隐状态             : [B, T_total, d]
文本输出                  : [B, M, vocab_size]
Talker 条件               : Thinker 隐状态 + 已生成文本 embedding + 历史语音 token
语音 codec token 输出      : [B, S, vocab_audio]
Token2Wav 中间表示         : [B, n_mel, T_mel]
BigVGAN 输出              : [B, T_wave]

```

#### 5.1.4 关键子模块详解

**A. Thinker**

1. **模块作用**

    - 负责统一理解多模态输入，并输出文本级语义结果。

2. **内部结构顺序**

    - 音频/视觉编码器分块处理 → 音视频按时间交错并使用 TMRoPE 对齐 → Transformer Decoder → 文本与高层表示。

3. **矩阵/张量维度变化**

    - 不同模态先被映射到共同隐藏维度 $d$，但序列长度 $T_i$ 仍由输入时长、帧数和分块策略决定。

4. **为什么这样设计**

    - TMRoPE 显式编码音频与视频的时间对齐；分块编码支持流式输入。文本是一个输出轨道，但 Talker 还直接读取 Thinker 隐状态，不是先把所有信息压成文字再做 TTS。

输入 token 化在公开报告中有明确时间尺度：

- 音频先重采样到 16 kHz，再计算 128 通道 mel-spectrogram，窗长 25 ms、步长 10 ms；音频编码器输出的一帧约对应原始音频的 40 ms。
- 视觉编码器沿用 Qwen2.5-VL 的 ViT。视频使用动态帧率；单张图片按两个相同帧处理。patch size 为 14，随后用 MLP 合并相邻 `2×2` token。
- TMRoPE 把旋转位置维度拆成时间、高度和宽度三部分。文本的三组 position ID 相同；音频的三组 ID 也相同且每个时间 ID 对应 40 ms；视频 token 的时间 ID 按真实帧时间推进，空间 ID 则记录网格坐标。
- 带音轨视频按 2 秒切块，每个块内先排列视觉表示、再排列音频表示。这里的“交错”是时间块级组织，不是每个音频帧和视频 patch 一一交替。

**B. Talker**

1. **模块作用**

    - 直接利用 Thinker 的高层表示与历史上下文生成流式语音 token。

2. **内部结构顺序**

    - Talker 同时读取 Thinker 高维表示和 Thinker 已采样文本 token 的 embedding，以双轨自回归方式生成离散语音 token；

    - qwen-tts-tokenizer 提供可流式解码的语音 code；

    - Token2Wav 使用滑窗 Flow-Matching DiT 把 code 变成 mel-spectrogram，再由 modified BigVGAN 生成波形。

3. **矩阵/张量维度变化**

    - 条件序列被映射到 Talker 自身的隐藏维度，再自回归输出 `[B,S,vocab_audio]`；Thinker 与 Talker 不要求具有相同宽度。

4. **为什么这样设计**

    - 分离文本推理与语音生成路径，以减少两个输出模态的干扰，并支持文本/语音并发流式输出。

滑窗不是“把整个历史都交给 DiT”。公开报告把相邻 code 组成块，并把当前块的感受野限制为 4 个块：2 个过去块、当前块和 1 个未来块。有限 look-ahead 会引入少量缓冲，却能让首包不必等待整段语音，同时保留局部上下文。

#### 5.1.5 训练方式与目标函数

Qwen2.5-Omni 不能概括成一次“全模态端到端训练”，公开配方包含清楚的阶段和不同目标。

**Thinker 预训练与后训练：**

1. 第一阶段锁定 LLM，分别对视觉、音频路径先训练适配器，再训练对应编码器，建立图文与音文对齐；
2. 第二阶段解冻全部参数，混合文本、图像、视频、音频以及带音轨视频数据；
3. 第三阶段加入长音频和长视频，把训练上下文扩展到 32,768 token；
4. 后训练再使用 ChatML 格式的文本、视觉、音频和混合模态对话数据做指令微调。

Thinker 的文本轨道仍是自回归交叉熵：

$$
\mathcal{L}_{\text{text}}
=
-\sum_t
\log p_\theta(y_t\mid x_{\text{multi}},y_{<t}).
$$

**Talker 后训练：**

1. 先用带多模态上下文和语音回答的数据做上下文续写/next-token 训练；
2. 再用 DPO 提高语音稳定性，偏好样本依据词错误率与标点停顿错误率等信号排序；
3. 最后做多说话人指令微调，提高音色可控性与自然度。

其基本语音 token 目标可抽象为

$$
\mathcal{L}_{\text{codec}}
=
-\sum_s
\log p_\phi
\left(
a_s
\mid
a_{<s},h_{\text{Thinker}},y_{\le k(s)}
\right),
$$

其中 $a_s$ 是语音 code，$h_{\text{Thinker}}$ 是流式到达的高层表示，$y_{\le k(s)}$ 是当时已经生成的文本。Token2Wav 的 DiT 另用 Flow Matching 目标学习 code 到 mel 的连续生成过程。

Thinker 与 Talker 被设计为可联合训练和端到端推理，但模块化职责仍然清楚：Thinker 负责文本序列与高层表示，Talker 负责语音 token，Token2Wav 负责声学重建。这里的“端到端”不表示整套系统只有一个网络、一个 tokenizer 或一个损失。

#### 5.1.6 优势、局限与下一代模型动机

- **优势**：模态覆盖广，交互形态更接近真实助手；Thinker / Talker 分轨减少文本与语音输出冲突，分块编码器和滑窗 Token2Wav 则直接服务于低首包时延。

- **局限**：该公开版本输出文本与语音，不生成图像或视频；多编码器、双解码轨道与流式同步使训练和部署复杂。

- **下一代动机**：进一步把生成侧（尤其图像/视频生成）也原生纳入统一主干。

#### 5.1.7 一句话总结

**Qwen2.5-Omni 的关键，是把"看懂世界"和"实时说出来"拆成 Thinker 与 Talker 两个协同系统。**

### 5.2 Ming-Omni

#### 5.2.1 要解决的问题（痛点 & 背景）

如果说 Qwen2.5-Omni 重点解决的是"全模态理解 + 语音表达"，那 Ming-Omni 更进一步追问的是：**能不能把全模态理解、语音生成、图像生成放进一套更统一的开源体系里？**

#### 5.2.2 总体网络架构图

```text
图像/视频 ── Qwen2.5-VL 视觉编码器 ─┐
音频 ─────── Whisper 音频编码器 ────┼─ 投影 + 拼接 ── Ling MoE（模态专用 Router）── 文本
文本 ─────── tokenizer ─────────────┘                         │
                                                             ├─ AR 音频解码器 ── 语音
                                                             └─ 多尺度 Query + Connector + DiT ── 图像
```

这是“共享感知/推理主干 + 专用生成器”的系统统一，而不是 Qwen2.5-Omni 那种共同端到端训练的 Thinker-Talker，也不是 Chameleon 那种所有输出都用统一离散 next-token 目标。

#### 5.2.3 逐层维度流（代表性示意）

```text
文本 token                 : [B, T_text, d_Ling]
视觉编码器 token           : [B, T_v, d_v] → [B, T_v, d_Ling]
音频编码器 token           : [B, T_a, d_a] → 线性层/卷积降采样 → [B, T'_a, d_Ling]
统一主干输入               : [B, T_total, d_Ling]
Ling MoE + 模态专用 Router : [B, T_total, d_Ling]
文本输出                  : 自回归文本 token
语音输出                  : 专用 AR 音频解码器 → 离散音频 token
图像输出                  : 多尺度 learnable query → Connector / DiT

```

#### 5.2.4 关键子模块详解

**A. Ling MoE 与模态专用 Routers**

1. **模块作用**

    - 让来自文本、视觉和音频编码器的 token 在统一 Ling MoE 主干中处理，同时缓解不同模态的路由冲突。

2. **内部结构顺序**

    - 专用编码器提取各模态 token → 对齐到主干 → 模态相关 Router 分配 MoE 专家 → 聚合输出。

3. **矩阵/张量维度变化**

    - 输入输出保持 `[B,T,d_{\text{Ling}}]`；Router 为每个 token 产生专家分数，形状可抽象为 `[B,T,E]`，随后按实现选择部分 Expert。

4. **为什么这样设计**

    - 不同模态的数据分布差异很大，使用模态专用路由可以减少它们竞争同一路由决策。视觉侧保留 Qwen2.5-VL 视觉骨干的架构但在图像与视频上重新训练，音频侧采用 Whisper，再经线性投影与卷积降采样接入 Ling。专家数量和激活规则必须以具体配置为准，原报告并未把这一组件命名为 AnyExperts。

**B. 模态专用生成解码器**

1. **模块作用**

    - 在共享感知/推理主干之后，分别承担自然语音与图像生成。

2. **内部结构顺序**

    - 语音：Ling 隐状态 → 自回归音频解码器 → 经 BPE 压缩的离散音频 token → 逆 BPE → 音频 tokenizer 解码。

    - 图像：Ling 的多模态语义 → `4×4 / 8×8 / 16×16` 多尺度可学习 Query 与 Connector → DiT diffusion 分支 → 图像。每个尺度都有独立 Query、网格位置编码和可学习起止标记，各尺度序列拼接后再由 Transformer 编码，使全局布局、中层结构与细粒度纹理分别获得对应容量。

3. **矩阵/张量维度变化**

    - 主干接口统一到 hidden states，但音频 codec/latent 与图像 diffusion latent 仍由各自生成模块定义。

4. **为什么这样设计**

    - “统一系统”不等于所有输出共享一个 tokenizer 或一个损失。Ming-Omni 通过共享 Ling 的语义条件再接专用生成器组合能力；公开版本还用 MSE 对齐 DiT 中间表示与最终语义表示。MingTok 是后续 Ming-UniVision 工作中的连续视觉 tokenizer，不应倒写成 Ming-Omni 原始架构。

语音分支的 BPE 作用在**音频 codec ID 序列**上：公开报告称它把约 50 Hz 的 token 率压到约 32 Hz，长度降低约 36%。BPE 变换本身可逆，所以不会额外丢掉 codec ID；但底层 neural codec 仍是有损声学表示，不能据此称整个音频链路无损。

#### 5.2.5 训练方式与目标函数

Ming-Omni 的训练应分成两个大阶段理解：

1. **感知阶段**：预训练、指令微调与对齐训练逐步把图像、视频、音频和文本接入 Ling，并通过模态专用 Router 与动态 loss 平衡缓解不同模态收敛速度冲突。
2. **生成阶段**：冻结多模态感知 LLM，平行训练音频解码器与图像生成组件。语音分支优化音频 token 自回归目标；图像分支只训练新增 Connector、多尺度 learnable query 和 DiT，并包含图像 diffusion 生成目标与多尺度表示对齐 MSE。原报告没有把该分支描述成 BAGEL 式共享主干 Rectified Flow，因此不应擅自套用 BAGEL 的 flow 方程。

因此文中所说“统一”主要体现在输入感知和共享语义条件；公开训练策略有意冻结 Ling，以免新增语音/图像生成目标破坏已有理解能力。

#### 5.2.6 优势、局限与下一代模型动机

- **优势**：公开报告覆盖图像/文本/音频/视频输入，以及文本/语音/图像输出，并共享 Ling MoE 感知与推理主干。

- **局限**：输出生成仍由外接专用模块完成，并非单一主干直接产生所有模态；多阶段冻结、MoE 路由和多种 tokenizer/损失增加了训练与部署复杂度。

- **下一代动机**：继续向更长视频、更强世界建模、更统一的内部时空表征推进。

#### 5.2.7 一句话总结

**Ming-Omni 的结构要点是“专用输入编码器 + 带模态路由的 Ling MoE + 专用语音/图像生成器”；统一发生在系统与主干层面，不代表 tokenizer 和输出头完全相同。**

### 5.3 Omni 对比 & 世界模型

#### 5.3.1 Omni 对比表

|模型|输入|文本输出|语音输出|图像输出|核心特点|
|---|---|---|---|---|---|
|Qwen2.5-Omni|文本/图像/音频/视频|是|是（流式）|否|Thinker-Talker、TMRoPE 与流式音视频处理。|
|Ming-Omni|文本/图像/音频/视频|是|是|是|Ling MoE 模态路由 + 专用音频与 diffusion 图像生成分支。|

#### 5.3.2 Omni 与“世界模型”是什么关系？

多模态理解与生成提供了构建世界模型所需的一些组件，例如：

- 理解当前状态的能力；

- 生成下一状态的能力；

- 跨模态统一表征能力；

但这还不等于世界模型。更严格的世界模型通常要学习状态随时间和动作变化的转移规律，能够在反事实动作下预测后果，并服务于规划或控制。一个会描述视频、生成漂亮下一帧的模型，可能只学到表面统计相关性。

世界模型关心的，不再只是"这是什么"或"画一张图"，而是：

- 给定一个场景和一个动作，接下来会发生什么？

- 这个预测能否同时以文字、图像、音频、视频多种形式表达？

#### 5.3.3 一句话总结

**Omni 解决“多种信号怎样进入和离开一个系统”；世界模型进一步要求“状态怎样随时间与动作演化”。前者可以成为后者的基础，但不是必然终点或充分条件。**

---

## 六、附录

### 6.1 核心公式速查表

|名称|公式|作用|
|---|---|---|
|VAE 重参数化|$z=\mu+\sigma\odot\epsilon$|可导地从分布中采样 latent。|
|正向加噪|$x_t=\sqrt{\bar{\alpha}_t}x_0+\sqrt{1-\bar{\alpha}_t}\,\epsilon$|一步采样任意时间步的噪声样本。|
|扩散训练目标|$L=\mathbb{E}[\lVert\epsilon-\epsilon_\theta(x_t,t,c)\rVert_2^2]$|让模型学会预测噪声。|
|从噪声估计干净样本|$\hat{x}_0=(x_t-\sqrt{1-\bar{\alpha}_t}\epsilon_\theta)/\sqrt{\bar{\alpha}_t}$|把网络的噪声预测转换为 $x_0$ 估计。|
|CFG|$\epsilon_{\text{cfg}}=\epsilon_u+s(\epsilon_c-\epsilon_u)$|组合无条件与有条件预测，增强条件控制。|
|Rectified Flow|$x_t=(1-t)x_n+tx_d,\ u^\star(x,t)=\mathbb{E}[x_d-x_n\mid x_t=x]$|用配对速度作回归监督，学习把噪声连续输运到数据。|
|视频 tubelet 数|$N=(T/P_t)(H/P_h)(W/P_w)$|估算未经 merge/压缩的时空 token 数。|
|音频帧数|$N\approx\lceil T_a/\Delta\rceil$|由时长和编码器帧步长估算音频时间位置数。|
|Cross-Attention|$\mathrm{softmax}(QK^\top/\sqrt{d_k}+M)V$|让一种模态的 Query 读取另一种模态的 Key/Value；$M$ 是可选掩码。|
|InfoNCE|$L=\frac{1}{2}(CE(S,y)+CE(S^\top,y))$|把配对图文拉近，不配对图文推远。|

### 6.2 模型对比总表

|模型|所属主线|核心输入|核心输出|关键词|
|---|---|---|---|---|
|ViT|理解|图像 patch token|分类语义向量|Patchify、CLS、Self-Attention|
|CLIP|理解|图像 + 文本|共享语义空间 embedding|双塔、投影、InfoNCE|
|LLaVA|理解|图像 + 文本问题|文本回答|MLP Connector、LLM|
|Qwen-VL|理解|固定 448×448 图像 + 文本|文本回答 / OCR / 定位|256 Query Resampler、坐标 token|
|Qwen2-VL|理解|动态分辨率图像 / 视频 + 文本|文本回答 / OCR / 定位|Naive Dynamic Resolution、2×2 Merge、M-RoPE|
|Qwen2.5-VL|理解|动态分辨率图像 / 动态 FPS 视频 + 文本|文本 / 框与点 / 结构化结果|Window Attention、实际尺度坐标、绝对时间|
|VAE|生成|图像|latent / 重建图像|下采样、重参数化、上采样|
|U-Net|生成|带噪 latent + 条件|$\epsilon/x_0/v$ 等连续预测量|多尺度、跳连、Cross-Attention|
|DiT|生成|带噪 latent token + 条件|扩散或 flow 的连续预测量|Patchify、AdaLN、全局注意力|
|Chameleon|统一理解与生成|文本 token + 离散图像 token 的混合序列|文本 / 图像 token|early fusion、统一自回归主干|
|Janus|统一理解与生成|SigLIP 特征 / VQ code + 文本|文本 / 图像 code|双视觉编码路径、统一 AR backbone|
|BAGEL|统一理解与生成|理解 token + 连续图像生成 latent|文本 / 图像|Decoder-only、MoT、AR + flow|
|Qwen2.5-Omni|全模态|文本/图像/音频/视频|文本 / 语音|Thinker、Talker、TMRoPE|
|Ming-Omni|全模态|专用编码器提取的文本/视觉/音频 token|文本 / 图像 / 语音|Ling MoE 模态路由、专用生成器|

### 6.3 名词索引

- **CLS token**：ViT 里负责汇总整图语义的特殊 token，可理解为"会议记录员"。

- **EOS token**：文本序列结束符；在 CLIP 文本塔中常作为整句表示读取位置。

- **Patchify**：把二维图像切成小块并变成 token 序列。

- **Projection**：把不同空间的向量映射到统一维度的线性层。

- **Cross-Attention**：Query 来自一种模态，Key/Value 来自另一种模态的注意力机制。

- **AdaLN**：用条件向量调制 LayerNorm 前后特征分布的机制。

- **VQ tokenizer**：把连续图像特征量化成离散 codebook token 的编码器。

- **CFG（Classifier-Free Guidance）**：通过条件与无条件预测差值放大提示词控制力的方法。

- **Resampler**：用固定数量可学习 Query 通过 Cross-Attention 读取可变或更长特征序列的压缩模块。

- **M-RoPE**：Qwen2-VL 把 RoPE 的部分旋转维度分配给时间、高度和宽度坐标，用一套位置机制兼容文本、图像与视频。

- **MoT（Mixture-of-Transformer-Experts）**：为不同模态或目标提供完整 Transformer 参数路径；与只复制 FFN 的常见 MoE 不同。

- **TMRoPE**：Qwen2.5-Omni 用于对齐音频与视频时间戳的多模态旋转位置编码。

- **Rectified Flow**：在选定概率路径上学习速度场，并通过 ODE 数值积分把噪声分布输运到数据分布的方法。

- **Neural audio codec**：把波形编码为一条或多条离散声学 code 序列并近似解码回波形的有损表示系统。

- **Block-causal attention**：块与块之间保持因果顺序，但图像、VAE latent 等块内部允许双向注意的掩码结构。

### 6.4 常见误区检查表

|误区|更准确的说法|
|---|---|
|“图像也变成 token，所以图像 token 与文字 token 相同。”|token 只是序列位置的统称；patch feature、VQ code 和带噪 latent 的离散性、信息量与训练目标都不同。|
|“投影到同一个隐藏维度就完成了跨模态对齐。”|相同维度只保证矩阵能拼接或做注意力；语义对齐来自数据、损失和共同训练。|
|“CLIP 已经在 token 级融合图文。”|原始 CLIP 是双塔全局向量对齐，编码阶段没有图文 Cross-Attention。|
|“视觉 token 越多越好。”|更多 token 保留更多空间细节，也会增加注意力、KV Cache 和延迟；Query 压缩正是在信息与成本间折中。|
|“音频 codec 的一个时间点只对应一个 token。”|残差量化 codec 常在同一时间位置产生多个码本 code；它们可以并行成多轨，也可以被串行化或再做 BPE。|
|“Diffusion 就是 U-Net，DiT 是另一种生成原理。”|Diffusion/flow 定义训练与采样过程；U-Net/DiT 是承载预测函数的网络骨干。|
|“换采样器不需要关心训练目标。”|采样器必须匹配 $\epsilon/x_0/v$/flow 参数化、时间坐标和噪声日程。|
|“Flow Matching 的网络在记忆每一对噪声与数据的直线速度。”|成对速度是回归标签；同一中间位置可能来自多对端点，MSE 最优解是给定 $x_t,t,c$ 的条件平均速度。|
|“统一模型一定只有一个 tokenizer、一个 loss、一个输出头。”|Janus、BAGEL、Omni 都展示了不同层次的统一；共享上下文、共享主干和共享目标是三件事。|
|“Omni 就表示所有模态都能输入和输出。”|必须逐项核对能力矩阵；Qwen2.5-Omni 接收四类输入，但公开版本只输出文本与语音。|
|“能生成下一帧就是世界模型。”|世界模型还要求对动作条件下的状态转移、反事实预测和规划有效，而不只是视觉上合理。|

### 6.5 主要参考资料

- [Vision Transformer（ViT）](https://arxiv.org/abs/2010.11929)
- [CLIP](https://arxiv.org/abs/2103.00020)
- [Visual Instruction Tuning（LLaVA）](https://arxiv.org/abs/2304.08485)
- [Improved Baselines with Visual Instruction Tuning（LLaVA-1.5）](https://arxiv.org/abs/2310.03744)
- [Qwen-VL](https://arxiv.org/abs/2308.12966)
- [Qwen2-VL](https://arxiv.org/abs/2409.12191)
- [Qwen2.5-VL](https://arxiv.org/abs/2502.13923)
- [Auto-Encoding Variational Bayes（VAE）](https://arxiv.org/abs/1312.6114)
- [Neural Discrete Representation Learning（VQ-VAE）](https://arxiv.org/abs/1711.00937)
- [SoundStream：端到端神经音频 Codec](https://arxiv.org/abs/2107.03312)
- [Denoising Diffusion Probabilistic Models（DDPM）](https://arxiv.org/abs/2006.11239)
- [Denoising Diffusion Implicit Models（DDIM）](https://arxiv.org/abs/2010.02502)
- [Latent Diffusion Models](https://arxiv.org/abs/2112.10752)
- [Classifier-Free Diffusion Guidance](https://arxiv.org/abs/2207.12598)
- [U-Net](https://arxiv.org/abs/1505.04597)
- [Scalable Diffusion Models with Transformers（DiT）](https://arxiv.org/abs/2212.09748)
- [Flow Matching for Generative Modeling](https://arxiv.org/abs/2210.02747)
- [Flow Straight and Fast: Learning to Generate and Transfer Data with Rectified Flow](https://arxiv.org/abs/2209.03003)
- [Chameleon](https://arxiv.org/abs/2405.09818)
- [Janus](https://arxiv.org/abs/2410.13848)
- [BAGEL](https://arxiv.org/abs/2505.14683)
- [Qwen2.5-Omni](https://arxiv.org/abs/2503.20215)
- [Ming-Omni](https://arxiv.org/abs/2506.09344)
